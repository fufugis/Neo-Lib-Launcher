# NEO-LIB progress

- **Full-screen Wall is source-complete** — Wall now becomes a lighter, full-workspace collection browser rather than squeezing beside Library. Two large choices switch between Side-by-side covers and Detailed list. Detailed rows show each game’s main genre, release date, last played date, measured install size, tracked playtime, source and personal rating; selecting a game goes to its normal Preview. Home and Library actions restore the normal sidebar workspace. Focused source checks pass; rebuilt Windows visual acceptance remains required.

- **One Wizard for Library work is source-complete** — Library now begins with Wizard only. Its first page clearly offers manual game add, folder scans, installed-launcher imports, metadata refresh and library tidy-up, so core library work begins from one dependable place. Categories remain in Library’s Categories control, and Tools keeps its separate Add tool workflow. Focused source checks pass; rebuilt Windows interaction acceptance remains required.

- **Reddit community shortcut is source-complete** — the official r/NeoLibLauncher button now sits beside Discord in the title bar and opens through the same normal-browser handoff. It remains icon-compact at narrow widths. Focused source checks pass; rebuilt Windows interaction acceptance remains required.

- **Original portrait cover art is source-complete** — Cover Wall now uses its portrait-card layout and prefers a dedicated portrait field. Steam receives the official `library_600x900` cover route from its known app ID; GOG carries its published vertical cover; reviewed refreshes preserve player-selected covers and backfill the new portrait field. If a provider has no portrait or an image fails to load, NEO-LIB intentionally renders a readable title/backdrop card rather than cropping a wide hero image into a false cover. Focused source checks pass; rebuilt Windows visual acceptance and a real Steam/GOG refresh check remain required.

- **Game capability badges are source-complete** — every Preview can now show source-declared controller support, single/multiplayer, co-op, cloud saves, Workshop, Remote Play and achievement availability in a compact icon-led panel. Steam reads its official store category/controller data during a reviewed metadata refresh; existing entries from other launchers surface only an exact directly stored feature tag. Tooltips retain the provider and original declaration. Achievement counts, when Steam publishes them, mean “available”, never falsely imply earned progress. The forthcoming opt-in achievement connectors remain intentionally separate because account progress belongs to the player and launcher. Focused source checks pass; rebuilt Windows visual acceptance remains required.

- **Preview playtime and install-size facts are source-complete** — every game preview now shows tracked time played and a compact install-size status. When NEO-LIB has a validated local executable but no size yet, the player can request a bounded scan of that game folder; the result, measurement time and partial-scan state are saved with the library entry. NEO-LIB deliberately shows “Not measured” rather than guessing for launcher-only or unresolved games. Focused source checks pass; rebuilt Windows scan/persistence acceptance remains required.

- **Home Widget foundation is source-complete** — every existing Home panel now has a stable first-party widget identity, Home segment and responsive minimum/default size contract. The current dashboard remains visually and functionally unchanged while its existing hide/reorder state keeps working. The package format and safety rules are documented: future community widgets will be isolated HTML/CSS/JS/WASM packages, never DLLs or native/Electron code. The next Home step is the visible player-owned grid, then the sandboxed community host; focused registry verification passes.

- **Home widget import and inspection is in development** — Home now has Import widget and Widgets controls. The manager lists every built-in widget and its visibility, while imported packages expose author, description, version, size needs and future requested permissions. Selecting `widget.json` validates and copies a package into NEO-LIB-owned storage without executing it. Community code remains disabled until the isolated host is built; verification is in progress.

- **Mid theme contrast is source-complete** — Generic Blue, Gaming and Modern now use raised medium-tone surfaces, panels and borders that visually belong with Generic Gray and Home in the Mid group. Their identity colours and readable light ink remain intact; only the excessive near-dark foundation and matching Theme Studio swatches changed. Focused source verification is next; rebuilt Windows contrast review remains required.

- **v1.7.8 is release-aligned and source-verified** — the complete v1.7.7 notes remain available as history, while a new v1.7.8 release body retains that full story and adds a dedicated summary of all 14 later hotfix commits. Package identity, About, in-app What's New, both READMEs, release workflow, hardening checklist, candidate inspector, legacy update fixture and Windows acceptance record agree on exact clean tag `v1.7.8`. The complete gate passes across 186 runtime files, 118 renderer modules and all 85 native commands; GitHub now needs to produce a green candidate before the public tag is created.

- **Optimize antivirus-compatibility repair is source-complete** — the user report was an Advanced Threat Defense behavioral block, and inspection found that opening Speed Up Gaming automatically ran one broad encoded PowerShell probe across processes, executable paths, GPU counters, WMI, gaming registry values and the power plan. That probe has been removed rather than asking players to whitelist it. Optimize now uses only the existing aggregate CPU/RAM reader, cannot list or close another program, links to Task Manager for per-process detail and retains direct Windows Settings handoffs without pretending their state was inspected. Focused Optimize, renderer and IPC tests pass without executing PowerShell, Task Manager, cleanup or a process action; rebuilt Windows/Bitdefender acceptance remains required.

- **Hard-title metadata recovery is source-complete** — refresh now begins with exact public-title discovery, expands joined executable names such as `MonolithBay`, tolerates changing Google and DuckDuckGo result markup, and can expand a surviving official itch creator page into reviewed title, description, creator, cover and screenshot candidates even when itch catalogue search no longer lists the game. Exact public evidence runs before optional AI, while the normal review step still prevents silent replacement. Focused provider fixtures and the complete pre-build source gate pass; rebuilt Windows/live-network acceptance remains required.

- **Preview hero visibility is source-complete** — hero artwork now measures both brightness and colour instead of brightness alone. Dark or desaturated banners receive proportional brightness/contrast/saturation recovery, vivid banners stay close to their source, and unreadable cross-origin samples use a safe mild recovery. The former full-width navy veil, bottom fade, accent wash and scanlines are substantially lighter while the strongest contrast remains behind the title. Player-selected hero art now wins over provider fallbacks. The complete pre-build source gate passes; rebuilt Windows comparison across muted/vivid games remains required.

- **Preview information order is source-complete** — Game Details now leads the information surface directly after status notices, with developer, publisher, release date and official website arranged in one compact responsive row on wide windows. About this game and its media gallery begin immediately underneath, removing the old long scroll to basic facts. The complete pre-build source gate passes; rebuilt Windows visual acceptance remains required.

- **Automatic description formatting is source-complete** — Preview now cleans HTML, BBCode, entities, invisible characters, broken spacing and obvious provider truncation without asking AI to rewrite or invent text. Embedded all-caps store sections become compact themed headings and ordinary prose is grouped into calm readable paragraphs instead of one oversized line or a staircase of sentences. Reviewed metadata is normalised before saving; manually authored copy remains under the player's control. The complete pre-build source gate passes; rebuilt Windows visual acceptance remains required.

- **All-launcher Recent Game Releases is source-complete** — the Home feed now covers every launcher NEO-LIB imports: Steam, Epic, EA, GOG, Ubisoft, Battle.net, Riot, Xbox, Rockstar and itch.io. Major launches rank first and remain for 14 days; smaller verified titles expire after 5 days and occupy at most four slots. Search is used only as an index to launcher/publisher-owned pages, previews/patches/pre-orders are rejected, and non-Steam results have a readable artwork fallback. The complete pre-build source gate passes; rebuilt Windows/live-network acceptance remains required. Standalone games continue through their per-game official news/update sources because no shared standalone catalogue exists.

- **Monochrome Bright theme is source-complete** — the Bright group now contains four themes. Monochrome uses a true white/pale-gray canvas, crisp black ink and controls, neutral borders, geometric grayscale ambience and quiet black particle marks. The Library receives the same atmosphere, while Effects intensity, motion cadence, Rest Mode and light-theme edge-glow limits retain their established behavior. Focused visual-boundary verification passes; rebuilt Windows contrast and visual acceptance remains required.

- **Library icon-only presentation is source-complete** — Visual Tweaks now prefers the main pane immediately to the right of the Library instead of covering the shelves it changes. A separate Icons only view removes game labels, category headers and the pinned shelf, preserves safe selection/context actions, and exposes dedicated icon-size, spacing and one-to-three lane controls. Standard row, typography, category and layout controls visibly disable in this mode, while locked private entries remain filtered out. Focused visual and renderer-state checks pass; rebuilt Windows visual/interaction acceptance remains required.

This is the short, human-readable view of the active work. It is updated as features move from idea to verified work.

The authoritative item-by-item status is maintained in [`WORK_QUEUE.md`](WORK_QUEUE.md). Open queue items must not be described as finished or release-ready.

## Mascot event expansion — source complete

- Fungist and FiFi now share a fuller but intentionally quiet event map: game-launch send-offs, manually added/imported library entries, private-category protection/unlock/panic confirmations, and controller connection changes while Controller Center is visibly open.
- Every new observation is independently switchable in Mascot Center. It obeys mascot visibility, UI sound/voice controls, Rest Mode and the shared voice gate; it neither records controller input/device identifiers nor exposes private category/game names.
- Existing health, favourite news, updates, completion and idle behavior remain unchanged. Pure privacy/event tests plus the full source gate are required before packaged acceptance.

## Release status

- **Cover Wall now exposes personal ratings in source** — rated visible games have a yellow upper-right cover tag with the player’s one-decimal score. It remains legible across every Wall density, sits above artwork/hover layers, and never appears for unrated or locked games. Source checks pass; rebuilt Windows review remains required.

- **Mascot resizing is isolated from interface text in source** — the 70–145% Mascot Center slider now resizes only FiFi/Fungist and character-owned glow/effects. Docked speech, alerts, completion/launch messages, hover help, chat, quick settings and major centre notices stay at their readable UI dimensions. The dock reserves the scaled character footprint to prevent a large mascot from overlapping the fixed-size bubble. Source checks pass; rebuilt Windows review at minimum/default/maximum size remains required.

- **Mascot alerts are readable and external-game Rest Mode is automatic in source** — when the local watcher confidently matches Valheim or another library game running outside NEO-LIB, the app now enters Rest Mode immediately instead of asking through a tiny button. A large centre-screen FiFi/Fungist notice states what was detected, what paused and that NEO-LIB wakes when the process closes. Serious PC alerts use the same major-event surface; routine mascot cards and speech remain docked but are wider, simpler and more readable. Focused source checks pass; rebuilt Windows visual and live-process acceptance remains required.

- **GitHub now produces the v1.7.7 Windows package** — the first clean-tag attempt encountered one transient ASAR fixture read, while the identical `main` commit was green. Ten local repetitions passed and an untouched GitHub retry completed successfully, producing a 311 MB Actions artifact and six files on the public v1.7.7 Release. The installed v1.7.7 app was also opened on Windows: Home/news, FiFi and the foreground Control Center menu rendered and responded. Future release uploads use the maintained Node 24 release action; the broader clean-profile, upgrade and feature acceptance checklist is still required.

- **The GitHub EXE blocker is repaired in source** — GitHub's Yarn install did not create pnpm's private `.pnpm` directory, but one build gate assumed it always existed and stopped before Vite could create the renderer. That gate now supports both Yarn/npm and pnpm dependency layouts, and the complete pre-build suite passes. Artifact upload uses the Node 24 generation of the GitHub action. A new unrestricted GitHub run is still required to produce and inspect the actual installer; the release tag must be the exact clean `v1.7.7`, not `v.1.7.7`.

- **The typography comfort pass is source-complete** — NEO-LIB now has one calmer app-wide hierarchy instead of many ordinary labels requesting maximum 900 weight. Compact labels and normal section titles resolve to 700; display-font heroes and 2XL-or-larger title/numerical callouts retain 800 for presence. This avoids a broad zoom control that could reintroduce clipping, and avoids yet another preference until the new baseline has been seen on Windows. The visual boundary gate protects the hierarchy; rebuilt visual acceptance remains required.

- **Visual polish and wake confirmation are source-complete after screenshot follow-up** — Visual Tweaks offers Windows default plus Neon, Petal and Pixel pointers. Reopening from the tray deliberately holds Rest Mode for 1.5 seconds with a centered blurred “Waking up” confirmation before resuming. Mascot quick settings can be dragged within the window and retain their local position; Mascot Center adds a 70–145% size control. The installed screenshot then exposed that the switch thumbs lacked a left anchor, so they are now clipped and positioned inside their tracks. Home Game Updates now puts updates, evidence warnings and launcher-managed context together inside one compact 320px scroll area, retaining every text line. Anime uses a lighter pink palette. The full source gate passes; rebuilt Windows visual acceptance remains required.

- **GitHub Actions Node-runtime maintenance is source-complete** — checkout and setup-node use v5 and artifact upload uses v7, so GitHub no longer needs to force their old Node 20 action runtime onto Node 24. A missing artifact message is downstream of a failed renderer build. The protected release feedback URL/key must be valid repository Actions secrets; this security requirement was not relaxed.

- **Manual and tray Rest Mode is source-complete** — the Library toolbar now has Rest Zzz beside Add/Wizard. It intentionally uses the same low-usage boundary as a launched game: ambient effects, sound, mascot activity, launcher discovery, news, deals and health polling pause together. The button becomes a theme-accent-glowing Wake up action. Close-to-tray now emits a native hidden/shown signal, so NEO-LIB sleeps while tucked away and wakes only when reopened. Source checks pass; a rebuilt Windows close-to-tray round-trip remains required.

- **Settings ownership is now clear in source** — Visual Tweaks no longer contains a category-management button, and Settings no longer carries dormant Mascot controls. Mascot Center is the only place for companion selection, visibility, voice, chat and observation choices. Settings is reserved for Sound, onboarding, app behaviour, optional AI, About and support; it also now includes a working preference to hide the sponsored deals strip. Focused source checks pass; a rebuilt Windows visual review remains required.

- **Patch notes, Discord feedback and Control Center polish are source-complete** — selecting Patch notes now always shows the current installed notes instead of an empty caught-up panel. The Control Center gear matches the navigation tabs in height and its compact text steps down from harsh 900 weight to a calmer 700/600. Feedback now uses the protected Discord relay as its sole normal delivery path; a local build missing relay values explains that setup problem rather than redirecting a report to GitHub. The real webhook remains off-device and off-repository. Focused source checks pass; a rebuilt Windows click/send test remains required.

- **Control Center dropdown delivery is repaired in source** — the gear menu previously handed its React portal through an animation wrapper, a fragile shape that can result in no rendered dropdown. It now mounts directly in the dedicated foreground portal and protects both pointer and mouse events from immediate outside-dismissal. The source boundary check locks this implementation; a rebuilt Windows click remains required.

- **The next candidate is cleanly versioned as v1.7.7** — inspection of the installed v1.7.6 archive found its native Steam correction but an older renderer without the Control Center click and hover-boundary repairs. Package identity, About, updater tag checks, workflow, release/acceptance records and the in-app patch-note heading now agree on v1.7.7. The patch-note content itself is unchanged. A fresh build and Windows install remain required before this is a released version.

- **Automated acceptance handoff is complete** — the full v1.7.7 source gate now locks launch safety, package identity, feedback/credential boundaries, 85 IPC contracts, persistence, launchers, metadata, privacy, providers, diagnostics, FiFi integration, operation recovery, visual boundaries, release-document truth and candidate inspection. The living Windows acceptance record names the remaining runtime proof precisely, including current Control Center, hover and Steam update cases. Further work now requires an unrestricted Windows build, physical/visual/audio testing, or publication approval—not more source restructuring.

- **Control Center, hover and Steam update correction is source-complete** — the new left gear now explicitly stays above Library artwork and consumes its own pointer press, delayed hover explanations measure themselves and flip/clamp inside the window, and Steam update cards require both remaining bytes and a live Steam update/download state. The Home handoff now uses Steam’s documented Downloads route. Source checks pass; a rebuilt Windows check should confirm the menu opens, edge hover text stays visible, and a completed Steam game clears from Home after Refresh.

- **Wizard Reliability Pass is source-complete** — the remaining folder-scan/review path now has the same escape hatches as launcher import. Fast scanning has a 32-second ceiling, Deep scanning 55 seconds, metadata review 28 seconds, and optional artwork caching 12 seconds. Player cancellation and window-close invalidate late work, and every terminal result returns to a readable retry/skip/folder-choice state rather than a permanent busy overlay. The full source gate passes; rebuild and run one real launcher and one manual-folder scan before accepting it on Windows.

- **Outside-launch game detection was hardened in source** — NEO-LIB no longer relies only on a saved launcher/bootstrap EXE. It first matches the exact executable, then safely recognises a real game executable only inside that game’s saved install folder. This directly fixes the Battle.net/Overwatch pattern where `OverwatchLauncher.exe` starts `retail\\Overwatch.exe`; an idle Battle.net process elsewhere cannot trigger Rest Mode. The watcher remains local, read-only and bounded to imported game folders. Desktop acceptance with the user’s live Overwatch install remains the final proof.

- **Mascot and controller control-center follow-up is source-complete** — Mascot is now a first-class Control Center destination, holding companion choice, on/off state, voice previews/volume, chat context and the exact warnings the player permits it to observe. The old visible Mascot areas were removed from Settings. Theme Studio advertises Custom theme as coming soon. Controller Center provides both Windows Bluetooth/device management and a Steam Input shortcut that refuses to open until Steam is already running.

- **Phase 1 Control Center is complete in source** — the gear now sits to the left of Home and opens one foreground-safe NEO-LIB menu: Themes, Visual Tweaks, Controllers, Mascot, Settings, Patch notes, update checking, feedback and a true full quit. Themes moved out of the oversized Settings window into a dedicated studio, including a clear Custom theme coming-soon entry; Visual Tweaks left the crowded Library toolbar; Controller Center became a first-class destination. TV Mode is deliberately shown as a Phase 2 promise rather than a fake toggle, while Lite Mode stays a separate Phase 3. The move retains the existing privacy-bounded controller detection and Windows device handoff; all 113 renderer modules, visual boundaries and 85 native commands pass their source checks.

- **v1.7.7 now has one coherent public story** — the GitHub Release body, root README, desktop README and in-app What's New entry now present the architecture rebuild and player-facing work in a structured form. The in-app modal shows a curated overview instead of 118 competing “major” lines, while the complete release document retains the detail across Library, metadata, privacy, Home, themes, mascots, Windows integration, diagnostics and packaging. The workflow will use that exact document when the accepted `v1.7.7` tag is published.

- **The v1.7.7 release pipeline now fails safely instead of silently dropping integrations** — GitHub cannot build a release without a valid HTTPS feedback relay and sufficiently long relay key, and the final package inspector proves those exact configured values reached the renderer without exposing them in its report. An offline Worker fixture now proves signing, replay/body limits, per-IP rate limiting, CORS and capped Discord forwarding. Optional Discord Rich Presence is prepared before provenance, uses a numeric-only generated file outside tracked source, and is checked inside the archive. Source and packaged output are scanned for known credentials; `.env` cannot enter the package and its temporary plaintext input is removed in a guaranteed cleanup path even when renderer compilation fails. Installer and portable hashes are published without local machine paths.

- **The renderer architecture is now frozen for acceptance** — the final root audit moved Auto-sort apply/undo into a tested workflow owner and removed four proven-dead remnants: the old unused bulk import, unused ordering helper and two refresh-progress values that could never change. `App.jsx` now contains no unused root bindings; the build gate will reject their return. At roughly 1,590 lines it remains the composition/wiring root, while feature rules have named state, service and component owners. Further reshuffling is deliberately stopped until real installed behavior is accepted.

- **v1.7.7 passes its complete source gate; the local managed build remains externally blocked** — every automated source suite passed after the architecture freeze. Vite was then invoked for a real renderer build, but Windows denied esbuild child-process creation with `spawn EPERM` before compilation. The package freshness hook correctly rejects the prior September renderer against the recalculated current-source fingerprint, preventing a misleading mixed installer. An unrestricted Windows build is now the next acceptance action, not more architectural movement.

- **Launcher detection can no longer hide an automatic import path** — running-client detection now has one small tested owner and can only offer the confirmation-first Wizard. The unreachable legacy silent-import branch and retired auto-import setting were removed from `App.jsx`; launchers already represented in the Library are ignored, while dismiss and 24-hour Ask Later behavior remain intact. The ten-client Wizard still owns scanning, review, deduplication, category creation, cancellation and timeout/rejection recovery. `App.jsx` is now roughly 1,640 lines.

- **The next build is v1.7.8 and preserves update discovery for old installs** — v1.7.3 already checks GitHub's latest published Release at launch, but its legacy parser cannot understand the historical dotted tag `v.1.7.5`. The current checker accepts both styles, while the release gate proves the exact clean `v1.7.8` tag is recognized by v1.7.3 and makes GitHub Actions reject mismatched/dotted release tags. The notification becomes live only after the verified v1.7.8 installer is published as GitHub's latest non-draft Release.

- **Preview information and hero ownership is complete** — About/Identity, verified game media, factual detail rows, hero title/release and precise personal rating now live in focused Preview components. `GameDetail.jsx` dropped from roughly 1,140 to 918 lines without changing launch, update or news behavior. Tests protect paragraph splitting, eight-image uniqueness/capping, fallback identity, all panel hooks, star-rating wiring and the new composition boundaries; the full source gate passes across 96 renderer modules.

- **Preview action ownership is complete** — after removing three unreachable legacy renderers, the full live toolbar and managed-tool menu moved to `PreviewActionBar.jsx`. Every action and foreground category popup remains wired, while `GameDetail.jsx` dropped from 737 to 517 lines. The launch-safety gate was deliberately redirected to the new owner—not weakened—and still proves that only the visible Launch control requests one-use native authorization. All 97 renderer bindings and the full source suite pass. Preview status cards are the next small boundary.

- **Preview status ownership is complete** — Steam build evidence, all-launcher live news, managed-tool readiness and update/history notices now have a dedicated status component and tested label calculations. Every native request, stale-result guard, error message and review path is preserved. `GameDetail.jsx` is now a focused 216-line composition component, down from roughly 1,140 before this Preview batch. The full source gate passes across 99 renderer modules. The next renderer audit has started on Sidebar’s live tree rows.

- **The Sidebar live tree and toolbar boundaries are complete** — category columns, category/game drag-and-drop, private-category presentation, pinned games and foreground menus now have one focused Library-tree owner, while the reusable opaque toolbar buttons, decorated tabs and launcher dropdown have a separate toolbar owner. The old unused launcher-pill renderer was removed. `Sidebar.jsx` fell from roughly 1,847 to 798 lines without changing visible behavior; split fixtures prevent category loss/duplication and the visual gate prevents either implementation from drifting back. All 102 renderer modules and the complete source gate pass; an unrestricted renderer build remains the Windows-environment checkpoint.

- **The App modal/overlay composition boundary is complete** — the root no longer renders thirty individual dialogs and transient surfaces inline. `AppModalLayer.jsx` now owns their exact presentation and routes existing state/actions through an explicit context contract, including Settings recovery, Controller Center, metadata review, private-category prompts, tutorial, feedback, playtime import, drag overlay and toast. Browser-preview seed data also moved to a deterministic pure factory. `App.jsx` fell from 2,415 to roughly 2,030 lines; permanent tests protect all important modal owners, controller handoff, overlay hooks and demo privacy. All 104 renderer bindings pass.

- **Metadata refresh/review now has one workflow owner** — `metadata-workflow.mjs` coordinates safe reviewed refresh, exact Steam app-ID locks, Battle.net product identity, bounded local title hints, cached artwork, accept-before-write, Tidy identity queues and confirmed bulk refresh. `App.jsx` only injects current state/native/UI actions and fell again to roughly 1,860 lines. New direct fixtures prove that normal refresh never silently replaces metadata, Battle.net ignores stale cross-store IDs, Steam stays locked, and bulk work remains confirmation/review driven. All 105 renderer bindings pass.

- **Category and private-library coordination now has one workflow owner** — `category-privacy-workflow.mjs` owns creation/edit/delete/reorder, assignment-only category removal, game movement, PIN setup/removal/unlock and the panic-lock transition. Direct in-memory fixtures prove private deletion rejects a wrong PIN, unlocked removal never deletes a game, regular-category cleanup cannot expose private assignments, and panic lock selects the safe Preview atomically. `App.jsx` is now roughly 1,755 lines and all 106 renderer bindings pass.

- **The Library Visuals architecture pass is complete** — the entire Visuals panel now has one focused component instead of living inside the already-large Library sidebar. All layout, game/category text, font, marker, spacing, texture, FX, motion and feedback controls remain connected, while `Sidebar.jsx` dropped from about 2,320 to 1,796 lines. A build-blocking boundary check prevents the panel helpers from drifting back into Sidebar and verifies every important control remains present. This is a maintenance change with no intended redesign; installed visual acceptance remains part of the wider release sweep.

- **Architecture maintenance has resumed with a behavior-preserving renderer batch** — `App.jsx` dropped from 2,545 to 2,418 lines. Hardware-tool bootstrapping, conservative launcher-path repair, mascot library context, launcher category identity, update-ledger bounds and mascot-inbox bounds now have focused owners instead of living in the composition root. Tests preserve player-owned tool paths, idempotent startup, all ten launcher categories/imports, path-free mascot context and bounded saved state. Controller navigation, Minimalistic and NEO Lounge are intentionally parked.

- **Controller Center C1 is source-complete** — Settings now opens a focused panel that detects gamepads only while the panel is visible, identifies the preferred device with a short local fingerprint, distinguishes two matching pads and shows live buttons/sticks without keeping input history. Pair/remove opens the exact Windows Bluetooth page through a narrow native allowlist, which also repairs the previously blocked Optimize Center Windows-settings links. Unsupported battery, wireless-strength and disconnect data is never invented. Source gates pass; rebuilt Windows and physical-controller acceptance remains open.

- **Controller navigation C2 has a dormant safety core** — standard pads now normalize into semantic commands with stick dead zones, delayed directional repeat, no held-button activation repeat, text-entry suppression and modal HOME protection. Its explicit start/stop adapter performs no work merely by being imported, and Launch controls are excluded until a dedicated controller hold-to-launch safety design exists. No global controller navigation is enabled yet.

- **The future experience model stays cleanly separated** — Default/Minimalistic controls interface density while Desktop/NEO Lounge controls presentation, so themes and library behavior remain shared. Controller Center is the first visible piece; controller-wide navigation, Minimalistic and Lounge remain separately gated milestones rather than one risky rewrite.

- **FiFi live integration is source-complete** — FiFi now appears beside Fungist in Settings and runs through the established companion host, retaining local alerts, AI chat, dragging/saved docking, completion and launch reactions, voice mute/volume, reduced motion and Rest Mode. Her articulated rig supplies blink, gaze, breathing, fins, tendrils, reactions and particles; all 27 separate recordings are mapped exactly once. Dedicated FiFi checks and renderer binding checks pass. A rebuilt installed-app visual/listening pass remains open.

- **Playtime ranges now respect the local day** — daily snapshots and Stats cutoffs no longer turn local midnight into the preceding UTC date. A positive-UTC-offset regression fixture covers the exact boundary that exposed the issue.

- **The rebuilt app now launches successfully** — the user confirmed the repaired Windows build opens again. This accepts the core blank-window recovery. Intro completion, Home/Fungist presence, build identity, restart and close-to-tray still need explicit confirmation before the broader startup gate closes.

- **CPU timing is aligned in source** — NEO-LIB previously reused the prior CPU counter snapshot, so later values described the whole 15-second refresh gap while Task Manager normally showed a much newer view. Each health read now measures a fresh one-second whole-machine interval and explains its sampling window. Repeated reads are tested; an installed side-by-side comparison remains open.

- **Privacy-safe Diagnostic Recorder is source-complete** — NEO-LIB now rotates a small local record of startup, renderer, Electron child-process, main-process and native-command failure classes. It deliberately cannot retain game names, paths, URLs, searches, payloads, messages, keys, PINs or private-category identity. Feedback exposes Copy report and Open log folder, attachment stays unchecked, and nothing uploads automatically. Privacy/rotation, all 83 IPC contracts, renderer binding, visual-boundary and release-hardening checks pass; the rebuilt Windows interaction still needs acceptance.

- **The installed blank-window cause is repaired in source** — the coherent v1.7.5 package reached Electron normally, but Fungist referenced one helper that had moved to its new model module without importing it. That immediate renderer exception explains why even the intro disappeared. The missing import and a second dormant unbound launcher label were fixed. A new gate checks all 79 renderer modules for unbound runtime names; startup now shows a recovery message and writes bounded local renderer diagnostics instead of leaving an unexplained empty window. A fresh unrestricted rebuild/install is still required before the runtime repair can be accepted.

- **The previous mixed-build problem was resolved, but that installer now predates FiFi integration** — the earlier rebuilt v1.7.5 installer carried fingerprint `3EED3299FF39` and one coherent Stage 9A source generation. Current source has since connected FiFi and changed its fingerprint, so the previous installer is now correctly considered stale. Rebuild before FiFi or final acceptance testing.

- **Mixed builds are now prevented and identifiable** — Settings → About shows a deterministic build ID, architecture label, Git revision and build time embedded by Vite from the exact runtime source. Electron Builder now verifies that full fingerprint before packaging, so Visual Studio or a direct builder call cannot silently combine current native code with an old `dist-renderer`. The isolated test changes source and proves the old output is refused; the real September 5 renderer is also correctly rejected. A fresh unrestricted Windows build is the next required checkpoint.

- **Architecture Stage 9 source hardening has begun** — the first proof-led deletion pass removed one silently shadowed HTML helper, eight unused metadata, Gemini and news helpers, and a deprecated no-output visual stub. A new source gate checks 143 runtime files for duplicate top-level declarations, broken local imports, version drift, missing release entries/assets, direct IPC-registration regression and the return of retired paths. The final ownership map, Windows acceptance list, performance record and rollback procedure are documented. Installer/portable builds, clean/upgrade smoke tests and same-machine performance measurements remain open, so this is not yet a release-ready milestone.

- **Architecture Stage 3 — contracts and safe failure logging complete; acceptance remains open** — all 85 native commands register through one duplicate-safe boundary and 28 explicit domain modules. Every one of the 53 payload commands rejects malformed or oversized renderer data before native work; the other 32 intentionally take no payload. All 85 commands validate results and turn malformed results, service throws and rejected promises into their established safe fallback shapes. Local diagnostics record only command, domain, failure type and error class—never payloads or error text. The complete regression gate, including all ten launcher fixtures, passes. Packaged Windows acceptance is pending.

- **Save operations now have one tested owner** — inspection, backup listing/creation, confined restore, common-folder detection and bounded searching moved from `main.js` into a dependency-injected service. Its real temporary-disk test caught and fixed an old self-conflict where the copy destination was created and then required not to exist. Backup/restore now copy directory children with no-overwrite behavior; occupied live folders remain untouched. Packaged Windows interaction acceptance remains pending.

- **Storage measurement now has one tested owner** — Home's user-triggered game/mod measurement and ten-minute cache moved out of `main.js`. Temporary-filesystem tests verify that missing targets, launcher executables, duplicate installs and misleading shared-library roots are excluded while valid mod totals, cache reuse and forced rescans remain compatible. No live library or game folder was read.

- **Optimize junk review now has one tested owner** — bounded temp/crash/archive discovery, immutable scan tokens and Recycle Bin dispatch moved out of `main.js`. Tests prove protected game/save paths, fresh files and small archives are excluded; duplicate, changed, unknown and stale tokens cannot be acted on; and the service has no direct-delete path. The test uses a fake Recycle Bin and removes no candidate file.

- **Optimize process controls now have one tested owner** — performance-result normalization, process safety snapshots and cooperative close dispatch moved out of `main.js`. Tests prove critical Windows processes, NEO-LIB itself, changed names and stale lists remain protected. An allowed close uses exact PID `taskkill` without `/F`; refusal is returned instead of escalating to a force kill. No test starts or stops a process.

- **Game launch and external-game watching now have tested owners** — one-use launch authorization, startup quarantine, local/cross-process cooldowns, exact spawn options, running-game lifecycle and exit reporting moved into a launch service. Exact-path external detection, helper filtering, locally launched exclusion, change-only events and fast-active/light-idle resume cadence moved into a watcher service. Direct tests cover the safety branches without launching, querying or stopping a real process. Packaged Windows launch/resume acceptance remains pending.

- **Architecture Stage 4 provider/update implementation is complete** — seventeen injected services now own store, public web, specialist metadata, candidate dispatch, Gemini, news, releases, owned feeds, update history, deals, source discovery, installed evidence, page versions and independent assessment. The final audit finds one feed route, no legacy provider caches in `main.js`, no hidden direct native/network dependencies in provider modules and no automatic metadata apply. The offline gate and bounded live smoke pass across Steam, GOG, public search, itch.io, Steam News and four deal sources. Only packaged Windows refresh acceptance remains unavailable because this managed environment refuses to start the renderer build helper with `spawn EPERM`.

- **Architecture Stage 6 renderer-state implementation is complete** — Library mutations, workspace navigation, global privacy redaction, metadata review queues, playtime, settings-derived visuals and persistence now live in small tested modules rather than one central component. Home still opens first, but the last valid Library and Tools selections are remembered for return navigation and restart. `App.jsx` has one preload gateway instead of 69 direct Windows bridge references, and its 400-line ambience/decorative renderer now has a separate visual component. Pure transition/privacy tests, JSX parsing and the full existing regression suite pass. Packaged navigation and visual acceptance remain blocked by the environment's `spawn EPERM` build restriction.

- **Architecture Stage 7 interface/visual source boundaries are in place** — changelog content, Settings controls, Home calculations and Fungist decisions no longer share files with all of their presentation mechanics. Library, Preview, Wizard, tutorial, news, stats, feedback and standard modal popups now use one foreground gateway; decorative navigation art has one non-interactive wrapper. A new renderer-wide check parses every component, prohibits stray portal owners and clickable decoration, and exercises privacy/health/recommendation/mascot behavior. The full offline gate passes; rebuilt Windows visual, focus, narrow-width, reduced-motion and theme acceptance remains open because Vite is blocked by `spawn EPERM`.

- **Architecture Stage 8 truthful-status source implementation is complete** — launcher scan/import, metadata review, Home/news refresh, weekly releases, game-update checks and storage scans now share unique operation IDs, bounded deadlines where appropriate, player cancellation, stale-result rejection and distinct success/partial/failure/cancel/timeout/unavailable outcomes. Empty results are no longer used to hide a failed news request. A bounded local journal records only safe operational facts, and Feedback attaches it only after the player checks an explicit option. Pure tests cover lifecycle races and forbid identity, path, query, key, message and private-category leakage. Packaged Windows interaction acceptance remains blocked by `spawn EPERM`.

- **Shared public-web discovery now has a tested owner** — DuckDuckGo/Google result parsing, raw public searches, game-specific search wording, fallback order and conservative web metadata synthesis moved into a second provider service. Tools/news callers keep their raw software or news queries, while game lookups keep the existing “video game” suffixes. Offline tests prove the behavior using injected HTML; no search engine was contacted. Specialist metadata providers, live-network acceptance and packaged Windows acceptance remain open.

- **Indie and visual-novel metadata now has a tested owner** — itch.io search/details, DLsite code/product lookup, VNDB mapping and Ryuugames fallback moved behind a specialist provider service. Offline fixtures preserve source URLs, title/creator mapping, Visual Novel identity, six-image caps and safe errors. Source ordering is unchanged and tests contact no provider. Candidate orchestration plus live-provider and packaged Windows acceptance remain open.

- **Metadata candidates now use an explicit source dispatcher** — list and expansion routes for all nine current candidate sources now pass through one allow-listed service. It normalizes queries once, forwards only the existing AI context, contains throwing or malformed providers and returns safe empty/unknown results. Candidate choice and Apply remain player-controlled. The full regression gate passes without contacting a provider.

- **Gemini now has an explicit privacy-bounded adapter** — its allowed model, game-metadata normalization and Fungist chat request construction moved behind injected HTTP. Offline tests lock down key/query errors, metadata field caps, compact history, sanitized visible-library context and output settings. Chat remains manual opt-in and no test sent a key, prompt or library snapshot online.

- **News confidence and public discovery now have tested owners** — language filtering, safe outbound URLs, dates, game-name matching and official-site labels moved into a pure normalization service. A separate provider now owns official-first public-news queries, DuckDuckGo/Google fallback, deduplication, two-item limits and its six-hour cache. Fixed-time offline fixtures preserve the existing behavior without contacting search engines.

- **Weekly releases now route through a tested provider** — SteamSpy momentum and Steam's New Releases shelf are merged, bounded, verified against store details and classified into major, noteworthy or popular fallback tiers by an injected service. Tests cover its seven-day window, cache, force refresh and offline failure. The old implementation body is unreachable but deliberately retained until the next consolidation deletion audit; packaged/live acceptance remains open.

- **Weekly releases are now fully consolidated** — the unreachable legacy implementation and its duplicate cache/date/concurrency helpers were removed after source-equivalence and full-main verification. The weekly IPC now has one provider-owned implementation path.

- **All three owned-news feeds now have tested providers** — Steam owns its batching, 30-minute cache, BBCode cleanup, image extraction and language/date filters. A separate service owns itch.io devlog RSS and GOG changelog parsing. Offline fixtures cover failures per game/source, preserving the unified Home feed’s Steam → itch.io/GOG → public-web priority.

- **Update-history pages now have a strict provider boundary** — the manually requested history reader permits only public HTTP/HTTPS URLs, bounds parsed text, deduplicates and sorts versions, extracts nearby date/summary evidence, caps results and compares them with the installed version. Offline tests cover invalid protocols and provider failure without reading user data.

- **Deals now have one tested provider owner** — Epic free games, Steam specials, Instant Gaming, GOG, Fanatical and Ubisoft discovery moved out of `main.js` without changing the deals channel or renderer objects. Offline fixtures preserve every threshold, cap, source mapping, timeout, duplicate rule, independent failure path and the populated-only one-hour cache. No store or affiliate URL was contacted.

- **Update scans now have a tested coordinator** — Home and Preview retain their exact order-insensitive request key, 15-second result cache, forced-refresh behavior and overlap protection behind a small injected service. Deferred fixtures prove identical requests coalesce, different inputs serialize, expired results rescan and failures clear for retry. The actual update evidence code was not changed and no live library was inspected.

- **Update-source discovery now has one tested owner** — saved/official URLs, public Steam notes, maintained Battle.net pages and carefully title-matched public results are selected behind an injected service. Fixed-time fixtures preserve URL deduplication, source confidence, the three-source cap, redirect cleanup and six-hour cache. Search failure keeps stronger known candidates and no live search ran.

- **Installed-version evidence now has a safety-tested owner** — local `.build.info`, Unity/config, filename and Windows resource clues moved out of `main.js`. Temporary fixtures enforce absolute paths, two nearby folders, no more than 24 candidate files and the 30-second startup quarantine. The fixed encoded PowerShell reader still receives the EXE path only as inert environment data; no real game was inspected or executed.

- **Update-page version evaluation is now pure and tested** — version/build discovery, numeric ordering, the 80-match ceiling and trusted-source preference moved out of the independent-game scan. Text-only fixtures prove official evidence outranks a higher search-only number, while search evidence remains usable when it is the only source.

- **Independent update assessment is now isolated and honest** — source fetches are bounded to nine seconds and 500,000 processed characters, evidence is cached for fifteen minutes, and available/current/attention/needs-evidence results have one owner. Cached pages without an explicit version remain `needs-evidence`; they can no longer turn into a false `current` result on the next scan. Installed-version parsing now retains complete alpha, beta and release-candidate suffixes.

- **Playtime timezone edge repaired** — Stats snapshots and range cutoffs now create local calendar keys directly instead of turning local midnight into a UTC date first. A positive-UTC-offset fixture protects the boundary; rebuilt Stats acceptance remains open.

- **Architecture Stage 2 — implementation/regression ready, Windows acceptance pending** — app-data paths and raw atomic I/O now have one owner, with library/settings/playtime governed by a separate schema-1 document service. It backs up valid data before migration/save, serializes competing saves, recovers a corrupt primary from last-known-good, preserves private and unknown fields, and refuses to overwrite unreadable or newer files. Window bounds now merge through the settings queue. Virtual-disk failure/recovery tests and the complete prebuild regression gate pass without accessing live AppData. The build was retried but Vite's helper still fails to start with `spawn EPERM`; no packaged migration or restart test has run, so the stage remains open.

- **Future architecture plan recorded** — `ARCHITECTURE_ROADMAP.md` defines ten independently reviewable stages with safety rules and exact acceptance gates. Stages 1 and 2 are automated-test complete but await packaged Windows acceptance; Stage 3 is underway through small contract-tested domain extractions.

- **Architecture step 1 — regression verified, desktop pending** — isolated all ten native launcher scanners behind an explicit dependency boundary, removing roughly 430 net lines from `main.js`. Captured the original results before moving code; 40 offline fixture runs and ten full-main integration scans match them, with the current 85 IPC endpoints registered. Launcher regression tests now run before renderer builds. Existing launch-safety, import and refresh checks pass. Build verification is blocked by `spawn EPERM` starting the build helper; the packaged Windows app has not been smoke-tested. This is a structural change only, not a fix for existing detection limitations. No user data or release version changed.

- **FiFi voice choreography and personal particles** — every one of the 27 stored MP3 filenames now has an executable performance script and live event mapping. Nine effect styles plus nod/double-nod/bow/presentation accents feed the articulated rig. Coverage and lifecycle checks pass; listening/alignment and installed visual acceptance remain pending.

- **FiFi animation rig — live in source, ready for installed visual review** — separate generated body, fin and tendril parts now drive independent blinking, gaze, breathing, hovering, twelve reactions and rest/reduced-motion controls. The shared mascot host now lets Settings select FiFi, preserves saved dragging/fly-return behavior and routes her separate 27-line voice pool. Offline lifecycle, asset, voice-map, CSS and JSX checks pass; installed visual/listening acceptance remains pending.

- **FiFi voice pack stays separate** — all 27 supplied FiFi recordings remain isolated from Fungist and are played only when FiFi is selected. Fungist's original mapping remains available unchanged.

- **Cleaner category headers** — launcher badge is no longer followed by the same launcher name in white. Alias/custom-name tests pass; custom names and private Unlock labels remain visible.

- **Launcher wizard repair — regression verified, desktop pending** — corrected confirmation portal mismatch and removed blocking online metadata/image lookups from local launcher imports. Added 30-second scan deadline, cancellation, stale-result guards and launcher ID isolation. Tests execute actual wizard/import/category functions against ten launcher fixtures and pass. Not a live reproduction of the user's EXE; installed EA/Epic scan and confirmation display still need acceptance. New games initially use local records; Refresh info fills metadata.

- **Preview backdrop fine-tuning** — main backdrop 62% to 56% opacity for slightly more visible theme FX; inner reading-card fills, typography and blur unchanged. Installed visual review pending.

- **Magical four-corner revision** — generated magical-corner-wisp-v2.png and integrated four mirrored uncropped corners without changing existing border lines. Transparency and component checks pass; preview regenerated for visual approval.

- **Industrial spark/resizing polish** — integrated three larger welding spark clusters and retained button-relative horizontal artwork scaling. Preview sections can now be dragged narrower for acceptance checks. Generation edits returned opaque backgrounds and were rejected; the selected fresh variant has near-transparent centre (alpha 1/255). Component/path/CSS checks pass; packaged visual testing remains pending.

- **Special buttons: chain revision** — generated and integrated industrial-chain-button-v3.png, replacing both the old frame and cropped machinery ornaments for Industrial. Magical corners corrected to upper-right/lower-left. Preview tests pass; latest visual approval pending.

- **Special buttons, stronger revision** — following positive user feedback on the preview, enlarged protruding cherry blossom details, added visible Industrial cog corners with warmer metal tones and stronger Magical rune/crystal accents. Reuses the existing illustrations, preserves opaque centres and foreground labels, and keeps the decoration slider. Preview regenerated; component/asset/CSS checks pass, new revision awaits visual review.

- **Reviewable game refresh — source and regression checks complete, desktop acceptance pending** — replaced immediate per-field writes and interactive Battle.net full-refresh bypass with a common candidate picker. Shows five results initially, then expands by five; displays titles/sources, protects existing data until Apply, supports selecting screenshots, and reviews bulk refresh one game at a time. Added offline tests for field isolation, identity preservation, paging/deduplication, exhaustion, cancellation, errors and shared routing. No real library changed during tests. Live provider retrieval and installed UI interactions are not yet verified.

- **Navigation artwork rebuild — awaiting visual acceptance** — the prior CSS-mask implementation still failed in the user's screenshot. Actual saved settings were Magical, 100% decoration and maximum FX, so low FX was not the explanation. Each button now receives explicit theme/opacity props and real HTML image URLs through NavButtonArtwork, eliminating the mask/custom-property image path. Static rendering of the actual components passes theme, opacity and asset checks; production CSS compiles. desktop-app/tmp/navigation-preview.html provides a three-theme preview without an EXE build. Browser rendering remains blocked by environment access/spawn errors; no visual success or exact previous runtime cause is claimed.

- **v1.7.0** — source commit pushed to `main` (`fc65001`). A GitHub Release tag and compiled installer have not been published yet.
- **v1.7.1** — local development batch; not release-ready yet.
- **v1.7.2** — previous development batch: Released This Week, safer Steam metadata identity, Home cleanup, and theme cleanup.
- **v1.7.3** — testing candidate: PC Power-Up, Hardware & graphics Tools, smarter update checks, and the latest Library/Home refinements. The authoritative item-level queue is in [`WORK_QUEUE.md`](WORK_QUEUE.md).
- **v1.7.5** — Library Refinement, Safety & Fungist testing candidate: includes the startup-safety recovery, Categories control centre, balanced Genre Intelligence v3 tags, reliable deliberate-Launch handshake, livelier Fungist idle behavior, smoother Balanced FX, a locally packaged 27-line Fungist voice palette, mascot-off quiet/recovery behavior, Library-only fonts, illustrated Special-theme decoration, and persistent Home update results. Release remains blocked for a rebuilt Windows check of: no-click startup, one deliberate game launch, the TrackMania helper correction/Fungist visibility, Categories manager behavior, visual polish, and real voice playback.
- **All-launcher news intelligence** — Home, the News panel, favourite alerts, and Preview now accept every named library game instead of filtering to Steam/GOG/itch identities. Existing Steam/GOG/itch feeds remain first choice; Epic, EA, Ubisoft, Battle.net, Riot, Xbox, Rockstar, standalone, and F95-style entries then receive a bounded official-site-first public-news pass and a clearly-labelled web-search fallback. Per-game six-hour caching plus a rotating fresh-check budget protects responsiveness and search services. Search evidence is news discovery only, never a verified launcher-update claim. Source-validated; rebuilt Windows/network verification remains required.
- **Battle.net refresh recovery** — Battle.net import and Re-fetch now carry the local product identity alongside the display name. Metadata resolves against a maintained Blizzard product catalogue before any Steam identifier or generic source; Warcraft III / Reforged codes and World of Warcraft variants map to their proper canonical Blizzard record. A manual Refresh bypasses the short Blizzard-page cache, retries current official art/copy, and retains a reliable Blizzard-backed description, publisher, website, genres, and tags if that page is temporarily unavailable. Source-validated; rebuilt Windows verification remains required.
- **Startup-only Fungist greeting** — “Welcome to NEO-LIB” is now reserved for one completed intro per desktop window. A welcome notice no longer travels through normal background-alert audio, so Fungist cannot unexpectedly replay it while NEO-LIB is already open. Source-validated; rebuilt Windows behavior verification remains required.
- **Library dropdown foreground repair** — launcher, Sort, Categories, and Refresh menus now use one forced foreground plane above the scrolling Library tree, and their surface is near-solid rather than decorative glass. Category text and game rows cannot paint over choices or show through them. Source-validated; rebuilt Windows visual verification remains required.
- **Special navigation decoration corrected** — Home, Library, Wall, and Tools are again distinct opaque controls, not one large artwork frame. Each Special theme now adds one small transparent illustrated corner flourish per button, kept beneath the label and icon. Source-validated; rebuilt Windows visual verification remains required.
- **Special button art now survives low FX** — the tiny static nav ornaments follow the Special-decoration slider directly and hide only at 0% or in Rest Mode. Lowering global visual FX for performance no longer suppresses them. Source-validated; rebuilt Windows visual verification remains required.
- **Battle.net Re-fetch now visibly completes** — when a maintained Blizzard identity is found for a Battle.net game, pressing Re-fetch info saves that exact result immediately instead of putting it behind the generic accept screen. Manual re-search stays reviewable, while field refreshes now carry Battle.net product identity too. Source-validated; rebuilt Windows behavior verification remains required.

## Completed locally for the next update

- **Library popup foreground repair** — Sort, launcher filter, Categories, Add, and Refresh now use an explicit toolbar foreground layer and one 100-level menu plane. Category headers, game rows, textures, and theme FX cannot paint through or appear above their open menus. Existing portal context menus and modal dialogs retain their higher global layers. Source-validated; rebuilt Windows interaction verification remains required.
- **Home private-lock status notice** — whenever one or more PIN categories are still locked, Home now shows a compact private-lock status card. It clearly explains that stats and news for those games remain hidden until they are unlocked in Library, including for an empty locked category. The notice disappears as soon as all private categories are unlocked. Source-validated; rebuilt Windows visual/interaction verification remains required.
- **Special navigation illustration-frame repair** — Anime, Industrial, and Magical now attach their actual generated transparent artwork as a dedicated frame around the entire Home/Library/Wall/Tools button bank, rather than relying on a pseudo-element that could render invisibly behind navigation chrome. The frame sits below labels and hit areas, is fully pointer-safe, and remains governed by the existing Special-decoration slider, FX level, and Rest Mode. Source/asset-validated; rebuilt Windows visual verification remains required.
- **Preview action-strip FX balance** — the dark Launch/action strip now uses a lighter 24% themed surface and reduced blur. Its own buttons remain readable, but the active theme background and FX can show through the surrounding bar again. Source-validated; rebuilt Windows visual verification remains required.
- **First-PIN Lock private guidance** — creating a private category now explains the readable red-outlined **Lock private** button at the top-right of Home. It clearly says the button re-locks every PIN category, redacts private content, and returns to a safe Library view; your PIN is retained and must simply be entered again to reveal the category later. Source-validated; rebuilt Windows interaction verification remains required.
- **Library toolbar readability** — Library action and filter controls now use a denser themed surface, while their open menus are near-solid. Add, Wizard, Visuals, launcher filter, Sort, Categories, Auto-sort, and Refresh remain legible above artwork, textures, and moving FX. Source-validated; rebuilt Windows visual verification remains required.
- **Unified category-header scale** — the Category text size slider now scales the category name, launcher/client badge, count, dot, arrow, drag grip, and options icon as one visual system. Independent vertical nudges are gone, so all header elements align cleanly on one centre line. Source-validated; rebuilt Windows visual verification remains required.
- **Protected category visual identity** — locked PIN categories now use the player’s chosen Dot or Backdrop marker treatment, matching ordinary categories without exposing their game count, entries, or launcher/client badge. Source-validated; rebuilt Windows visual verification remains required.
- **Library text-boundary repair** — Library is now a contained resizable pane, with its tree, rows, and game-title column clipped to their usable width. A game name can use two lines but cannot visually spill into another row or the Preview area; hover retains the full title for exceptional names. Source-validated; rebuilt Windows visual verification remains required.
- **Fungist hover-bubble boundary repair** — browser-controlled mascot hover text has been replaced with a compact in-app bubble above Fungist. The saved dock now clamps against both Fungist’s footprint and the bubble at compact window sizes, so neither can leave the viewport. Source-validated; rebuilt Windows visual verification remains required.
- **Home privacy layer + Lock private** — Home now receives a privacy-safe placeholder for every game in a still-locked PIN category. Home keeps non-identifying figures such as playtime and storage size, but replaces names, art, client details, paths, update content, news, and active links with a lock tile carrying only the protected category name. Protected cards cannot open Preview, a browser, launcher downloads, or folders; unlocking the category restores the original record instantly. The readable red-outlined **Lock private** button on Home re-locks every PIN category and moves immediately to a random safe Library Preview, re-filtering Wall and Home at the same time. Source-validated; rebuilt Windows privacy/interaction verification remains required.
- **Wall private-category unlock controls** — Cover Wall now shows a protected-category strip with one padlock button for every still-locked PIN category. Entering a category’s correct PIN unlocks only that category and then populates its games in the Wall; private games never appear merely from switching views. Source-validated; rebuilt Windows interaction verification remains required.
- **Fungist speech-bubble cleanup** — routine companion voice lines now appear in one compact, text-sized bubble with a small tail, without a large “Fungist says” panel, quote treatment, heavy glow, or duplicate line inside a notice. Actionable alerts remain full cards when they need a player decision. Source-validated; rebuilt Windows visual verification remains required.
- **Private category lock cleanup** — locked categories now show one clear lock control and a clean Unlock label, rather than repeating lock icons across the row. Their protected game count stays private until unlocked. Source-validated; rebuilt Windows visual verification remains required.
- **Home weekly focus pass** — the separate Your Week card is gone. Its hours played, games touched, today, and Library total now live in a compact summary strip inside the fixed Top 5 Played panel. What Should I Play uses the freed half-width slot with smaller artwork and concise reasons. This Week’s News is now a stronger pinned Home destination with an accent top edge, bigger headline/icon, clear date context, and larger readable story cards. Source-validated; rebuilt Windows visual verification remains required.
- **Tools metadata intelligence** — Tools now have their own software-aware metadata path rather than the Steam/game importer. NEO-LIB reads the selected executable's normal Windows icon and file identity first, checks a recognised vendor profile where available, then uses one bounded public official-software lookup for an unfamiliar app. Add Tool and Re-fetch info show an approval view with name, publisher, version, category, description, source, and evidence. Existing incomplete GPU-Z/CPU-Z and other tool records receive a bounded fill-only pass when Tools opens; custom data remains untouched. Source-validated; rebuilt Windows visual/network verification remains required.
- **Fungist background-app diagnosis** — high-usage notices now offer a player-requested, read-only Windows process snapshot that reports leading CPU/RAM users outside NEO-LIB and calls out clear game/client process names. This supports unimported games such as Overwatch without injection, overlay, account access, process control, or background polling. Source-validated; rebuild and Windows interaction verification remain required.
- **Home compact paired-card layout** — Recent Sessions + My Best Games, Your Week + Gaming Chronicle, and Library Health + Storage now share half-width space on wide windows while retaining each card’s own hide/reorder controls. Dragging live-reflows cards and shows the correct horizontal/vertical insertion line; compact widths return to one column. Source-validated; rebuild and Windows interaction verification remain required.
- **Special button-frame artwork** — replaced faint/placeholder Special-theme button decoration with three dedicated transparent illustrated frame assets: Anime blossom vine, Industrial gears/hazard machinery, and Magical rune/crystal framing. Both navigation and Preview actions use them at a visible slider-governed level while interactive controls remain above the art. Source/asset-validated; rebuild and Windows visual verification remain required.
- **Foreground-fog repair** — corrected the permanent window-edge glow from an above-interface layer to the background FX plane. It remains pointer-safe but can no longer visually veil any buttons, labels, menus, or text. Source-validated; rebuild and Windows visual verification remain required.
- **Library regular-weight correction** — normal Library type now resolves to 400 regular instead of 500 semi-bold, while Fat remains the deliberate 800 option. The change remains scoped to Library game/category names only. Source-validated; rebuilt desktop visual verification remains required.
- **Home section architecture** — Top 5 Played remains fixed at the top, followed by a deliberately smaller, fixed **This Week’s News** rail. The rest of Home is grouped into Play & history, News & updates, and Library & PC care. Sections have their own drag handles and reorder independently; panes drag only within their owning section. Storage and Gaming Chronicle are safely split from the old combined pane, preserving a previous hide choice. Source-validated; rebuilt desktop interaction/visual verification remains required.
- **Released This Week fallback** — the discovery feed now joins SteamSpy’s current player/review signals with Steam’s actual New Releases shelf. It prefers Major releases, falls back to Noteworthy releases with verified early momentum, then to clearly-labelled Popular new releases from the current shelf so a quiet AAA week does not leave Home empty. Source-validated; rebuilt desktop/network verification remains required.
- **External-game low-usage handoff** — a high/elevated PC-use alert can now ask Fungist to scan for a known library game running from Steam, Battle.net, Epic, or another normal client. The scan compares only local executable paths—never launcher accounts, game memory, injection, or network data. When it finds a match, Fungist names the game and offers a clear one-game **Enable low usage until it closes** action. While resting, the exact process is checked every eight seconds so NEO-LIB resumes automatically soon after the game exits; idle checks remain light at thirty seconds. Source-validated; rebuilt Windows behavior verification remains required.
- **Library-only type choice** — the Library Visuals panel now has five system-installed font stacks with independent Fat and Cursive controls. The choice affects only game/category wording in the left Library, leaving Home, Preview, Tools, and Settings unchanged. Source-validated; rebuilt visual verification remains required.
- **Illustrated Special decorations** — Anime, Industrial, and Magical now use real transparent illustration assets in addition to their ambient treatment: a cherry-blossom vine, graphite machinery/hazard detail, and a rune-light flourish. They remain non-interactive, follow the same Special-decoration slider, and disappear completely in Rest Mode. Generated with the built-in image workflow; source/asset-validated, with rebuilt visual/performance review still required.
- **Special-art composition repair** — the new illustration assets now sit on an explicit full-screen background layer with a safe stacking level, stronger normal-FX presence, and larger control details. The same slider, FX level, and Rest Mode boundaries still govern them. Source-validated; rebuilt visual review remains required.
- **Special navigation-frame repair** — the main Home, Library, Wall, and Tools bank now uses the actual transparent Anime blossom-vine, Industrial machinery, or Magical rune asset as a shared, visible frame around the button group rather than compressing it inside each tiny pill. Buttons stay above the art with clear labels, full click targets, and a restrained themed edge. The existing Special-decoration slider, FX level, and Rest Mode boundaries still govern it. Source-validated; rebuilt visual review remains required.
- **Special Preview-action frame** — the game Launch/action strip now receives the same real artwork as a restrained outer-edge frame. Every action is deliberately stacked above the art so labels, hit areas, and the primary Launch button remain clean and usable. Source-validated; rebuilt visual review remains required.
- **Preview glass rebalance** — Preview’s main reading panel, story, media, identity, details, path panel, stat cells, and action strip now use roughly 80% of their former surface strength with reduced blur. Theme art and animation are visible again behind the content while contrast-protected text and borders remain readable. Source-validated; rebuilt visual review remains required.
- **Category Dot marker repair** — launcher labels no longer replace the active Dot marker. Every normal launcher-labelled category now reserves a separate, z-layered coloured dot after its collapse arrow, before the launcher badge; Backdrop and None are unchanged. Source-validated; rebuilt visual review remains required.
- **Visuals control-board layout** — the former tall one/two-column Visuals list is now an intentional three-lane desktop grid: Object sizes & Layout, Text & category, and FX. Each family receives a solid themed header tab and fixed visual home; responsive breakpoints fall back to two then one lane on smaller windows. The popover is wider but remains clamped/draggable inside the app. Source-validated; rebuilt visual review remains required.
- **Global UI foreground contract** — the main application shell now explicitly layers Title bar, Library, Home, Preview, Tools, deals, text, buttons, menus, and inputs in front of all background/FX layers. Sidebar content has a matching local foreground rule above its theme artwork and texture. Ambient art, particles, edge glow, textures, and Special decoration stay pointer-safe and visually behind UI. Source-validated; rebuilt visual review remains required.
- **Remembered Fungist dock** — Fungist now has a player-adjustable lower-right perch with a bounded 360px left/up safety area. Click-hold dragging saves a local `fungistDockPosition`; automatic alert, chat, and launch-flight targets are calculated from it and resolve back to it when the action ends. Dragging suppresses the click-to-chat action and cleans up listeners safely. Source-validated; rebuilt desktop visual/interaction review remains required.

- **Release-tree cleanup** — removed one disposable root build log containing only an obsolete path error, one unreferenced duplicate mascot sleep asset, and now ignores ordinary generated logs. Required source, active mascot assets, voices, theme artwork, and build-safety scripts remain included for the upcoming commit. Source-validated; no app behavior changed.
- **Feedback fallback** — a build without the optional private Discord relay now offers an ordinary public GitHub report route rather than exposing relay environment-variable instructions to players. The selected report opens as a prefilled GitHub issue with only the player’s typed text plus version, theme, and platform. Source-validated; rebuilt desktop interaction verification remains required.
- **Preview identity placement** — moved the Game Identity genre/tag panel back inside the About this game reading card. The description now has a strict independent text column beside it from compact desktop widths upward, so each final line ends before the identity panel begins and can never run behind or beneath it. The separate media column is now reserved solely for larger game images. Source-validated; rebuilt desktop visual verification remains required.
- **Settings recovery + larger Preview media** — restored the established shared modal layout after the newer Settings-specific sizing route rendered a blank gray shell in the packaged app. A Settings-only recovery boundary now prevents any future Settings control error from blanking NEO-LIB: it gives the player a safe return to Library and an immediate bug-report action. Preview's right-hand gallery now has a 460–560px desktop column, roughly doubling its image area while preserving readable game details. Fungist now gives failed AI requests an honest top-bar Feedback reminder plus a direct **Report a bug** action. Source-validated; rebuilt desktop interaction/visual verification remains required.
- **Special-theme interface decoration** — expanded the Special decoration system from a faint distant-only layer into visible control-level identity. Anime receives blossom-vine/leaf curls, Industrial receives structural hazard rails/rivets, and Magical receives spell arcs/star dust around navigation and action surfaces. The effects are non-interactive, respect the existing decoration slider plus per-theme FX level, and vanish during Rest Mode. Source-validated; rebuilt desktop visual/performance verification remains required.
- **Settings width correction** — fixed the shared modal max-width conflict that let viewport safety override a modal’s intended size. Settings now uses a dedicated 43rem content cap, sized for its four 148px theme columns with breathing room while still clamping inside smaller windows. Source-validated; rebuilt desktop visual verification remains required.
- **Library text + Preview gallery correction** — removed the dark bordered Library nameplate entirely, retaining title wrapping without a backing surface. Preview no longer centers into a narrow reading column: it starts against the Library side, leaves story/details left, and gives up to eight verified local/source-owned media items a dedicated right-side gallery. Source-validated; rebuilt desktop visual verification remains required.
- **Working update-card action** — Preview and Home now use a dedicated native Steam Downloads action for launcher-confirmed Steam updates instead of the generic external-link bridge that correctly blocks game protocols. The full card is actionable, never launches a game, and shows a visible error if a future launcher lacks a safe queue adapter. Source-validated; rebuilt Windows interaction verification remains required.
- **Windows-aware Optimize settings** — Optimize now detects the Windows family, release, and build from Windows itself, displays a matching manual setting path, sends Background capture directly to the supported Game DVR/Captures page, and explains build-specific GPU scheduling availability. Source-validated; rebuilt Windows 10/11 routing verification remains required.
- **Mascot safety + Home continuity** — hiding Fungist now also mutes all companion voice while preserving the previous voice preference for restoration; Settings gives clear recovery guidance and keeps every switch on one aligned rail; the left Library receives a low-key FX-aware theme-art layer; and completed Home update results persist through Game Preview navigation until a newer scan replaces them. Source-validated; rebuilt Windows interaction/visual verification remains required.
- **Anime artwork + dark-surface comfort pass** — replaced Anime’s SVG/manga outline overlay with an original fully rendered 2D moonlit shrine-and-cherry-blossom background, preserving a quiet centre for UI readability. Synthwave, Midnight, Ocean, Crimson, Anime, Colorful, and Industrial retain their accent identities while their near-black base/box surfaces are lifted into deliberate dark gray, navy, graphite, or wine-gray layers. Source/asset-validated; rebuilt Windows visual review remains required.
- **Special-theme personality pass** — moved Anime to Special beside Industrial and renamed Colorful to Magical (internal saved theme ID unchanged for existing users). A new Settings decoration-visibility slider controls sparse theme-owned edge actors: Anime petals, Industrial cogs/rivets, and Magical spell arcs/sparks. These are background-only, use no polling, and disappear with FX/Rest Mode. Source-validated; rebuilt Windows visual/performance review remains required.
- **Rest Mode clarity + tutorial/title-bar polish** — Optimize now reassures players that seeing NEO-LIB while browsing is expected and that a tracked game automatically pauses its non-essential effects, animation, sound, monitoring, and content checks; matching context appears in the Game Ready detail view. Stabilized the tutorial navigation callback so Preview no longer flickers as settings persist, and unified Feedback/Discord/Coffee into one calmer theme-aware title-bar action family. Source-validated; rebuilt Windows interaction/visual review remains required.
- **Library row readability + arrival markers** — standard and two-column Library rows now carry a compact accent-edged full-name plate that wraps long titles rather than truncating them. New imports/manual additions receive a small New badge that is cleared only by a deliberate Library-to-Preview selection; existing libraries are quietly migrated as already seen. Source-validated; rebuilt desktop visual/interaction review remains required.
- **Launchers hub rename** — the former Friends rail control is now an honest Launchers hub. It only checks ordinary local client status and opens the original Steam/Battle.net/Epic/EA/Ubisoft client; no friend-list or chat wording remains in the active surface. Existing custom client paths remain compatible and future saves use the clearer launcher setting name. Source-validated; rebuilt desktop interaction review remains required.
- **Category-arrow scale correction** — the category expand/collapse glyph now uses the exact category text size and its button grows/shrinks proportionally around it, preserving a usable pointer target. Source-validated; rebuilt desktop visual review remains required.

- **Compact theme-tile standard** — Settings theme tiles now use one fixed 148px label-safe width, based on the longest current label plus breathing room instead of stretching across the window. Labels no longer ellipsize; narrower windows keep the content within the modal with horizontal scrolling. Source-validated; rebuilt desktop visual review remains required.
- **Theme-swatch motion calm-down** — theme bars in Settings now use one 48-second restrained colour drift rather than fast attention-grabbing movement. Source-validated; rebuilt desktop visual review remains required.
- **Wall route repair** — removed the redundant normal-Library route after selecting Wall. That route reset `libraryViewMode` to Preview immediately after the Wall selection; the grid now remains active until the player opens a tile or returns to Library. Source-validated; rebuilt desktop interaction verification remains required.
- **Fungist ground-FX alignment** — separated fixed horizontal centring from the animated scale transforms used by the neon pad and centre streak. The pulsing effect now remains directly beneath the character. Source-validated; rebuilt desktop visual review remains required.
- **Mascot opacity + Preview media calm-down** — removed Fungist’s pose-entry opacity animation, so idle/blink/speech/smile/sleep/completion swaps stay fully opaque while existing movement/glow continues. Preview restores a compact source-owned carousel limited to two images and removes the repeated in-paragraph screenshot breaks, leaving game descriptions as readable uninterrupted prose. Source-validated; rebuilt desktop visual review remains required.
- **Fungist overlay + library-aware Oracle** — removed the circular mascot pod in favour of a transparent full-character overlay with a reactive neon ground pad, aura, hover sparkles, and mood/action FX. Manual AI sends now attach a bounded visible-library snapshot containing names, genres/tags, ratings, and playtime only; paths, saves, accounts, processes, and locked Private games stay out. Local typed commands resolve `Launch {game}` and `Launch a random {genre} game` without an AI key, but present a named guarded confirmation button instead of auto-launching. Source-validated; rebuilt Windows visual, command, and consent-text verification remains required.
- **Fungist visible speech delivery** — all real Fungist voice events now publish one shared local speaking state containing the exact spoken line, a readable speech bubble, and a matching happy/thoughtful/concerned/urgent/celebratory delivery animation. The companion receives this from automatic alerts, chat, task completions, launch celebrations, support, and Settings previews; the tutorial mirrors the same event inside its own overlay. Source-validated; rebuilt desktop voice/visual playback remains required.
- **Home theme composition repair** — removed the accidental all-text shadow/stroke treatment that made ordinary Home copy fuzzy. Normal neutral text is now clean; only small coloured status/accent copy receives a dark readability edge. Home is rebalanced into a medium-gray workspace with deeper slate boxes, light-gray normal copy, restrained light-gray structure, and blue/gold saved for signals, glows, and FX. Source-validated; rebuilt desktop visual review remains required.
- **Fungist fluidity pass** — increased the docked companion from 98px to 118px and raised him slightly above the permanent bottom rail. His transform-only base motion now cycles more smoothly, blink timing is shorter and irregular, and idle smiles, curiosity, stretch, sparkle, and greeting gestures happen more often. The existing Rest Mode guard still removes all companion timers and rendering while a game runs. Source-validated; rebuilt desktop visual/performance verification remains required.
- **Mascot sound-settings cleanup** — removed the redundant synthesized Fungist soundscape preview from Settings. Global UI Sounds/Sound Pack remain available, while the companion’s voice toggle, volume, and line preview stay together as the focused audio control. Source-validated; rebuilt desktop Settings visual verification remains required.
- **Centered Preview reading pass** — the redundant standalone screenshot carousel is retired. Preview now uses one centered, less-transparent reading surface; existing verified screenshots remain as editorial visual breaks within longer descriptions, so theme animation stays visible around the page edges instead of competing with a second column. Source-validated; rebuilt desktop visual verification remains required.
- **Workspace, Wall, Private unlock, and companion finish pass** — Home is now the only Home dashboard route. Library restores last-played/first game, Tools restores last-used/first tool or shows an honest empty state, and Wall remains its own workspace until tile selection deliberately opens Preview. Wall now supports 3×3–10×10 centered landscape cards and prefers existing wide header/capsule art. A locked Private shelf gives a clear `Unlock {name} ({count})` action without exposing protected rows. Fungist lifts above chat in a ready pose, uses a short cheer flight to the actual Launch button after a confirmed manual launch, and the supplied voice library now contains 27 local clips with mapped completion, update, support, chat, and hand-off roles. Theme atmosphere art was regraded to be visible at normal FX levels while remaining under UI and entirely absent in Rest Mode. Source-validated; rebuilt Windows visual/interaction/audio verification remains required.
- **Startup update-scan root fix** — the Windows executable version reader no longer places a library EXE path after PowerShell’s `-Command` switch. A fixed script is encoded, while the target path is bound through a process-only environment value and read with `Get-Item -LiteralPath`, preventing the inspected game from becoming executable command text. Both automatic update-scan callers now wait 35 seconds, PE-resource inspection has an independent 30-second startup quarantine, and overlapping warm-up requests coalesce into one scan.
- **Launch-safety build gate** — `npm run build:renderer`, `build:win`, and `build:portable` now stop before compilation if the encoded version reader, inert target binding, preload authorization route, native one-time launch token, or visible Launch-button handshake is missing.
- **Fungist false-Rest fix** — live v1.7.4 testing identified TrackMania² Valley’s legacy `ConvertAssistant.exe` target as the reason Rest Mode hid Fungist. Launcher EXE selection now rejects assistant/converter/importer/editor/benchmark/diagnostic helpers; the external-game watcher ignores legacy helper targets; and an approved launcher rescan repairs those existing records from app ID/product identity without replacing a valid custom executable.
- **Launcher update-trust repair** — a launcher-confirmed Steam manifest result now ends checking for that game; it cannot be overwritten by a lower-confidence executable/public-page comparison. Other known launcher installs are logged as launcher-managed while their safe pending-download adapters are still being expanded, rather than falsely promoted to an update. A standalone/repack only follows independent comparison when it has no launcher ownership identity, regardless of metadata source.
- **Update Intelligence evidence resolver** — Home and Preview now key their short update cache to the exact set of games requested, so a one-game Preview can never reuse another game’s library-wide result. Independent installs read a broader but still bounded set of nearby Version/JSON/config/XML/manifest formats; saved and official sources outrank search discovery; source confidence and attempted routes are saved in the local ledger; and Home offers targeted **Resolve checks** controls for the games that still lack enough proof. If a source is unreachable or protected, the game is explicitly retained as needing evidence rather than quietly appearing clean.
- **Reliable Launch-button handshake** — a real launch press is now captured in NEO-LIB’s isolated preload only when it begins on the explicit visible Launch control, then consumed immediately to request a short-lived native authorization. This replaces a fragile Electron window-level input timestamp that could reject genuine presses while retaining startup quarantine, one-time tokens, cooldowns, single-instance protection, and the no-background-launch guarantee. A rejection now reaches the player as a clear notification rather than appearing to do nothing.
- **Fungist liveliness + smooth Balanced FX** — Fungist is slightly larger, has a cohesive 3D-rendered idle/blink/fly/sleep/completion pose family, and adds sparse curiosity, stretch, smile, and sparkle moments using only pose/transform animation; no video/GIF decoder, continuous canvas, extra poll, or Rest Mode activity was added. The Balanced Visual motion stage is now smooth rather than stepped and saves work by reducing costly particles, edge glow, blur/layer count, and ambient opacity; Calm applies the stronger reduction while menus and button feedback remain untouched.
- **Safe post-intro Fungist greeting** — after the visual intro overlay has fully left, Fungist now uses a renderer-only welcome bubble, friendly raised-hand 3D pose, and one brief larger greeting motion before returning to his dock. It does not call game launch, update scan, external link, or launcher code, and real attention alerts take precedence.
- **Startup, bounds, and FX recovery** — the boot intro now owns a single completion path and keeps the latest callback in a ref, so library/settings startup renders cannot restart its three-second timer or strand its exit frame. Tutorial begins only after intro completion; the post-intro Fungist greeting waits until a first-run tutorial closes, eliminating voice beneath the intro. Tutorial layout now uses actual viewport-safe height and internal scroll. The shared Modal plus all current draggable metadata, News, Stats, Tidy Up, Visuals, and category surfaces receive viewport drag constraints and safe initial placement. Modern effects now ignore retired `synthGridEnabled` / `particlesEnabled` flags that could otherwise make all visual FX disappear after an upgrade. Source-validated; rebuilt Windows startup, bounds, FX, and Fungist visibility checks remain required.
- **Fungist voice library** — all 27 user-supplied MP3 lines are now referenced through package-safe local Vite paths rather than a web service. They are intentionally mapped across welcome/tutorial introduction, favourite news and game updates, NEO-LIB version updates, yellow/red PC attention, deliberate game starts, guided actions, small/large completions, explanation requests, chat acknowledgement, slow chat thinking, and recoverable chat failures. A single active HTML audio element plus shared per-line/event cooldowns avoids overlap and background decoding; the independent voice toggle/volume and preview map live in Settings, while UI Sounds/No Sound and Rest Mode silence every line. Source-validated; a rebuilt Windows playback pass is still required.
- **Library controls: clearer collapse, scoped Sort, and Cover Wall** — category headers now have an explicit solid 28px expand/collapse arrow. The new Sort menu preserves Custom drag order or dynamically sorts each visible category / Flat List by Alphabetical, Date Added, My Rating, Most Played, or Last Played. A top-pane **Wall** switch turns the main area into a 3×3–10×10 cover grid with no persistent Preview; a tile opens the normal Preview and locked Private games remain excluded.
- **Categories control centre** — the Library’s former one-click flat-list control is now an explicit **Categories** menu. Its visible On/Off switch clearly shows whether category shelves are active; its manager can add, edit, or remove collections in one place. Removing a category only clears that category assignment, returning affected games to Uncategorized—no game record, media, saves, or play history is deleted. The guarded bulk action removes regular categories only and deliberately preserves Private categories and their protection.
- **Confirmation-first launcher imports** — each supported launcher now asks before scanning. If NEO-LIB finds prior imports, a second comparison states exactly how many detected games already exist and offers only the new entries; a fully imported launcher says so and adds nothing. Legacy running-launcher detection now opens this same Wizard path rather than silently importing in the background.
- **Theme readability source pass** — an RGB contrast audit found low-contrast accent text in Vaporwave Day, Daybreak, Mint, Modern, and Home. Decorative accents are now separate from their small-text companions, status words receive a stronger edge, and Home’s panels/copy are darker and calmer. A rebuilt Windows visual audit across all themes and window sizes remains open before this pass can be declared fully verified.
- **Theme artwork foundation** — Home, Generic Gray, and Generic Blue now have original, low-contrast 16:9 atmosphere artwork in the visual FX layer. It is centered beneath all UI, fades with the existing effects level, uses a single long transform-only drift outside Calm motion, and is completely omitted by Rest Mode. The broader all-theme artwork and particle-language pass remains in progress.
- **All-theme artwork source pass** — every current theme now has a purpose-built 16:9 background environment: Synthwave, Vaporwave Day, Daybreak, Home, Mint, Midnight, Ocean, Crimson, Anime, Gaming, Modern, Industrial, Colorful, Generic Gray, and Generic Blue. Each has theme-specific grading, remains beneath all UI, follows the existing FX intensity, uses only a long transform drift outside Calm, and is omitted entirely by Rest Mode. A rebuilt Windows visual/performance audit across all themes remains mandatory before the pass can be called verified.
- **Preview editorial treatment** — Game Preview now puts description copy inside a labeled reading card with steadier typography and release context. Longer descriptions can use only that game’s existing fetched screenshots as narrow visual breaks between paragraphs; there is no random search or generated-image fallback. A rebuilt desktop visual check remains open.
- **Library maintenance redesign** — Refresh now opens a transparent choice between missing/older metadata and a separately confirmed full refresh. Each calculates its target count first, excludes manual overrides, keeps the remaining queue alive if one lookup fails, and exposes compact `done/total` progress in the Library. Tidy Up remains a dedicated diagnosis/repair flow. Source-validated; a rebuilt desktop interaction check remains open.
- **Auto-sort review flow** — Auto-sort now requires three direct genre-profile matches before proposing a shelf, caps proposals at four, removes broad Adventure/Simulation fall-through, shows every assignment, supports shelf opt-in/out and one-click per-game exclusions, lets the player select individual incomplete games for genre enrichment, deliberately reuse a matching category or create a separate `Auto · …` shelf, caps a game at two new shelves, and offers a transaction Undo. Existing categories are not removed. A representative Windows interaction test stays open.
- **Genre Intelligence v3 balance** — direct tag display now keeps a maximum of eight gameplay-first labels. Storefront feature badges (such as Steam Cloud, Trading Cards, controller support, Workshop, and Remote Play) are suppressed; sparse external metadata receives a compact non-invented fill from its confirmed subgenre/playstyle profile. A taxonomy migration refreshes existing games on the next enrichment pass without touching Library category assignments.
- **Optimize Center** — a new bottom-right action beside CPU/RAM opens two animated, theme-aware power-up rows and remembers each tool’s latest result. Speed Up Gaming samples real per-process CPU and memory, shows GPU engine activity and its responsible processes when Windows exposes them, protects critical Windows/NEO-LIB processes, and explains Game Mode, GPU scheduling, recording, power-plan, and restart status with honest pros and trade-offs. Windows settings open in their native page; no hidden tuning is applied.
- **Safe Junk Review** — an on-demand bounded scan checks known Windows temporary/crash locations plus large old archives/installers beside configured games. Every exact file links to its folder and must be individually marked inspected before selection. Two confirmations, including typed authorization, are required; approved files go to the Windows Recycle Bin, never through recursive permanent deletion.
- **Post-play rating prompt** — a session lasting at least fifteen minutes can gently ask for the game’s one-decimal private rating. The player can rate, snooze for another session, or permanently suppress the prompt for that game.
- **Richer Preview news** — preview news now uses an article image when its public feed supplies one, then falls back to the game’s existing hero, background, screenshot, or cover art.
- **GPU setup assistant** — the first desktop launch reads Windows’ ordinary adapter list and adds a Hardware & graphics group in Tools: GPU-Z, CPU-Z, and the detected NVIDIA/AMD/Intel control centre when its local executable is found. If a vendor centre is not installed, the shortcut honestly opens Windows Graphics Settings instead. It never changes drivers, GPU settings, or the registry.
- **Managed GPU-Z / CPU-Z** — missing utilities are muted and say Set up required. Their compact setup menu can validate a manually located executable or, only after the player clicks Install, download from TechPowerUp/CPUID’s official site. GPU-Z is placed as its official portable executable in NEO-LIB’s managed tools area; CPU-Z opens CPUID’s visible installer without silent flags or elevation.
- **v1.7.3 test-fix pass** — NVIDIA Store apps are now discovered through the normal Windows Start-app list; the Home theme is a deeper neutral gray; and Category Backdrops scale tightly around their text. Update Intelligence now reads Blizzard `.build.info` when present, discovers a small set of relevant web sources automatically, and never calls a game current merely because a page lacked an explicit latest version.
- **Optimize test-fix** — Safe Junk Review no longer restarts itself after showing results. The Game Ready sample interval is now 15 seconds and the external-game executable-path watcher is 30 seconds, reducing unnecessary renderer updates and Windows-management process wakes.
- **Home + AI test-fix** — Home has larger content and artwork with a reserved control gutter so the drag/hide controls no longer cover headings. Gemini fallback has been consolidated onto `gemini-2.5-flash`, validates returned metadata, gives useful failures to the picker, and has a one-click Settings connection test.
- **New NEO-LIB icon everywhere** — the supplied neon power artwork has been rebuilt into a multi-size Windows ICO for the EXE, app window, taskbar, desktop and Start shortcuts. The running app now uses the matching stable Windows application identity, while installer upgrades safely notify Explorer to refresh icons without clearing caches, pins, or user data; a companion transparent tray mark prevents a blank notification-area icon.
- **Fungist companion core** — the new optional mascot uses the existing Game Ready health sample (no duplicate CPU/RAM poll), replaces watched-favourite news popups with a docked HEY bubble, links verified favourite updates and NEO-LIB updates, joins the tutorial, and offers opt-in stateless Gemini chat that sends only text manually submitted by the player. Rest Mode hides Fungist and pauses the feature completely.
- **Fungist animation + soundscape pass** — compact transparent poses now cover idle standing, blink, smile, shock, sleep, flight, and completion. He uses a gentle docked hover, low-key sparkles for ordinary attention, neon flight trails and outward firework sparks for a heavy alert, and explicit green/yellow/red health-status glow states. Six synthesized Web Audio cues—Good Ding, Hey, Welcome, Warning, Attention, and Completed Ding—play only for deliberate interface events and obey UI sounds/No sound; no GIF/video, looping audio, decoded buffers, or additional health poll were introduced.
- **Fungist reactive controls + AI model foundation** — Settings now provides a clearly marked current AI model and a backend allow-list, which is passed to Fungist chat and the AI metadata routes without allowing unimplemented provider IDs. The player can independently control high-PC alerts, PC checks, favourite news, favourite updates, NEO-LIB updates, completion celebrations, and idle naps. Health alerts reset only after recovery so Fungist can react again to a later, genuine high-use state.
- **Fungist attention hygiene + history** — notification types now use deliberate cooldowns (five minutes for a red PC alert, fifteen minutes for a yellow PC check, six hours for each ordinary event), so real state changes can still surface without repeated nagging. Every shown notice is retained in a bounded local Inbox; right-click Fungist for Inbox/Quick Settings, including direct control of companion visibility, PC alerts, news/updates, and celebrations. Each live alert now has a “Why am I seeing this?” explanation, and successful NEO-LIB actions produce a short result-labelled completion response.
- **Latest v1.7.3 test feedback** — Fungist’s public pose paths are now portable-app-safe, fixing the visible fallback text in packaged Electron builds. The chat surface reports key readiness before sending and opens Settings when a Gemini key needs to be added or tested. Home has been darkened to a neutral gray workspace with stronger blue/gold contrast and a subtle dark text edge for readable coloured labels; Game Ready plus its CPU/RAM green, amber, and red state text now has an explicit stronger shadow over the status rail.
- **Current v1.7.3 polish pass** — Home now uses a more energetic gray/blue/gold balance with a stronger dark text edge. Fungist has quicker but sparse idle blink/smile movement and a clean transparent sleep pose. The guided Visuals state is controlled directly so it closes cleanly, while the tutorial mascot no longer overlaps navigation. Library can now switch between Categories and a flat visible-games list without exposing locked Private titles. Independent installs can surface an amber comparison-needed card when a public patch is found but their installed build version remains unreadable; this is intentionally not presented as a confirmed update.
- **Fungist sleep presence** — Resting Fungist now breathes with a slow transform-only rise/fall, a quiet aura pulse, and a drifting `zZz`. It uses no GIF/video decoder, timer-driven canvas, or continuous particle field, and remains disabled in NEO-LIB Rest Mode.
- **Genre Intelligence v2** — broad-only identities are now treated as incomplete rather than “done.” Existing source evidence migrates to a broader exact-match taxonomy, Steam retains up to thirty ranked direct community tags, and deterministic tag combinations refine identities such as Sandbox + Building → Sandbox Builder and Roguelike + Dungeon Crawler → Roguelike Dungeon Crawler. Auto-sort and Tidy Up can now run an approval-first **Enrich source tags** pass for these entries; it never reads loose description words or changes Library categories.
- **Update evidence recovery** — when a standalone game does not expose a version in its own bounded files, NEO-LIB can now read its Windows executable version resource as a local clue. This may surface a public patch for comparison, but is intentionally too weak to mark the game current or claim a confirmed update by itself.
- **Guided onboarding + NEO-LIB Mascot** — the tutorial now actively switches through the real Library, a safely selected Preview when one exists, Tools, the open Visuals popover, Home, Game Ready, and Fungist rather than only describing them. Home’s tour calls out Top 5 Played, News/Game Updates, and Library Health/Storage. The companion controls are renamed **NEO-LIB Mascot** and now have a theme-style visual picker showing Fungist plus a clear future-companion placeholder; Fungist is raised 38px to keep the Friends area unobstructed.
- **Mascot recovery guard** — the visibility switch now lives in the NEO-LIB Mascot section as **Show NEO-LIB mascot**, so a hidden companion can always be restored from the place the player expects. The context-menu route to full settings explicitly preserves `fungistEnabled: true` before opening Settings, preventing a right-click settings handoff from leaving the visible companion disabled.
- **Fungist Oracle chat** — the companion now keeps a bounded, scrollable local conversation with clear You/Fungist bubbles; Send clears the composer immediately; Enter sends while Shift+Enter adds a line; and Clear erases the local record. Only the latest twelve compact messages are supplied with the next intentional Gemini request, so follow-up questions remain coherent without automatic PC/library/process/account attachment. Its new Oracle prompt is cheerful and mystical but disciplined: a greeting is a warm sentence and question, normal replies stay within roughly three short sentences, and detailed guidance appears only when asked.
- **Wording fit standard** — fixed interface labels, beginning with Library category headers, now use their available height and wrap at natural word breaks rather than discarding the end of the label with an ellipsis. Counts and controls stay aligned with the first line, so narrow sidebars remain clean and readable.
- **Battle.net metadata reliability** — Battle.net imports now canonicalize short/legacy Windows display names against a bounded Blizzard product map before the general fetcher runs (for example, `Overwatch` becomes `Overwatch 2`). Automatic matching now rejects weak unrelated Steam, GOG, and itch.io results rather than stopping the resolver. When a first title clue still fails, NEO-LIB tries bounded local executable/folder/readme title hints, then its public sources and optional Gemini fallback. Text-first AI/web results receive a safe single-page public artwork pass for hero, cover, background, and any clearly exposed gallery images; uncertain art is not invented.

- **Rating System v2 reset** — existing personal ratings are deliberately cleared once on first run of the new system, so old whole-star choices are not silently treated as equivalent to precise decimal ratings. New ratings remain one-decimal values.
- **Fractional star fill** — the preview rating control now fills each star by its actual decimal share: for example, `3.5` displays three full gold stars and one half-filled star, rather than only three whole stars.
- **Battle.net metadata enrichment** — known Battle.net products now use a bounded map to their official public Blizzard/Battle.net product pages before generic sources. This supplies the official title, description, artwork, website, and direct tags without login, account access, or launcher writes; unmapped products still use the normal picker/search path.
- **Provider tag visibility** — Game identity now keeps a compact direct-source tag group in addition to core genres and subgenres, so a game can read as `Action` plus `Third-Person Shooter`, `Action Roguelike`, `Co-op`, and `Multiplayer` rather than collapsing to the broadest label.
- **Home interaction overhaul** — Top 5 Played is a fixed centred Home feature that can still be hidden/restored; Library Health is a dedicated detailed panel; and pane ordering now previews the placement in real time with a glowing active handle, dimmed neighbours, animated layout movement, and an insertion line.
- **Stronger suggestions and Friends** — What should I play? now uses larger cards and states why a game is timely, unplayed, or worth rediscovering. Friends has a larger, more readable, less-transparent theme-aware surface.
- **Independent update discovery** — refreshes now inspect only nearby, bounded Version/Changelog/Readme files for an explicit installed version and compare it to a saved update page or public game website. A game without enough evidence is reported as needing setup rather than incorrectly being called current.
- **Background update intelligence** — version evidence is warmed shortly after library startup and whenever a game is launched. It safely considers launcher manifests, saved metadata, executable/version naming, and bounded nearby Version/Changelog/Readme/Config files, then checks official/public sources in a small parallel queue with a short cache. A verified update blinks in a dedicated Preview bar directly above the news alert.
- **Update checklist** — every conclusive check is saved locally per game as current, pending, available, or needs-evidence. Home and Preview show only real available/pending alerts; a game drops away automatically once its launcher/local version evidence reports it current again.
- **Library precision pass** — the first-game gap now reaches a true compact zero safely, Backdrop headers scale their padding/border with category text size, and a new NEO-LIB window begins at 75% usable width by 90% usable height before later restoring the player’s own bounds.
- **External-launch Rest Mode detection** — NEO-LIB checks ordinary Windows process paths for a known library game launched from Steam, Battle.net, Epic, or another client. It never treats an idle launcher as a game and performs no injection, overlay, memory inspection, or credential access. The current handoff offers the player a clear one-game Rest Mode action before it pauses the launcher.
- **Storage Control fix** — scans now ignore non-file launcher targets, measure a shared game-install folder only once, and keep the completed result while moving between Library and Home. The scanner never walks a whole drive: it checks only configured local game folders, with up to three folder walks at once for a quicker result.
- **New Mid theme: Home** — a comfortable light-gray workspace built around sharp black/white structure, light-blue window edges and effects, plus a warm yellow/gray architectural blend pattern.
- **Onboarding refresh** — first run now uses a short, current seven-step guide for importing, Hidden privacy, Home, game controls, customization, hover help, and Rest Mode. A new theme-aware hover-tip layer explains titled controls after one second throughout the launcher.
- **Mid-theme balance** — Generic Gray and Generic Blue now belong to Mid; Generic Blue has a brighter navy/blue palette.

## Completed locally for v1.7.1

- Home **Library Health** card with a colour-coded total score, issue chips, and a direct Tidy Up action.
- Larger Home news cards with Steam artwork when available.
- Home pane arrangement: drag panes into order, hide individual panes, and restore them from the Home header. News opens in-app before offering a Full story link; **My Best Games** shows top ten personal ratings alongside Metacritic where available.
- Missed shell fixes now implemented locally: Friends moves into the bottom of the selected game preview; Coffee sits beside Discord; labelled Settings follows Visuals in Library controls; Refresh/Tidy is grouped with Auto-sort; the sponsored rail is centred, permanent, and no longer contains Coffee or a dismiss action; five-star library frames use theme accents; preview rating reads **My rating** with larger stars.
- Cover art in Home Top 5.
- Game Ready thresholds tuned to yellow at 65% and red at 85%.
- **Rest Mode while gaming** is now on by default for games launched through NEO-LIB: it disables ambient effects, animations, UI sounds, health polling, launcher scans, background news/deal checks, and social refreshes until the tracked game exits. The Game Ready footer clearly shows **NEO-LIB RESTING** and the active game; the feature can be disabled in Settings.
- Library scroll space reserved above the Game Ready footer.
- **Save Game Folder** entry point in both the preview action bar and game right-click menu, with per-game folder selection and open-folder access.
- Safe, local-only **Save Backups**: backup records are stored inside NEO-LIB app data; recovery refuses to overwrite a non-empty live save folder and can create a separately named restored folder instead.
- User-triggered **older save finder** that searches a chosen drive/folder by game-name matches and requires the user to review every candidate.
- Home **What should I play?** card using local play history, ratings, added dates, and already-fetched patch news.
- Home **Storage Control Centre** with an explicit, read-only game/mod folder scan.
- Home **Gaming Chronicle** for local add / play / rating / update events.
- Conservative **Launch Doctor**: only after two immediate failures or very short launches within ten minutes; it checks the configured target and offers nearby executable candidates without changing files automatically.
- Distinct per-theme particle language rather than a shared sparkle look, plus three new Settings chimes (Aurora, Ember, Harbor) and three Visuals textures (Weave, Brushed, Stardust). Brushed replaced the non-seamless Topography tile.
- Visuals control reorganisation: Object sizes, Text & category, FX, and Layout groups; direct icon-position and category-marker sliders; category Backdrop intensity is tied to Category glow and remains translucent at max.
- Follow-up Visuals polish: Category marker `None` now removes both dot and backdrop; Brushed replaced the non-seamless Topography tile; Anime is pink-forward with original manga line art; Pro is renamed Industrial with animated safety-light geometry and metal sparks, without CRT scanlines.
- Library Health now counts all supported description fields and its Review action lists the actual affected games. Save Game Folder now automatically checks bounded, common Windows, game-folder, and Steam Cloud mirror locations; it never changes a save path until the user selects a result.

## Completed locally for v1.7.3

- **Released This Week** Home pane: a selective, rearrangeable release feed that verifies official Steam store dates and only shows full games with meaningful early player, review, or launch-reach signals. It displays artwork, source, date, and an inclusion reason; caches for six hours; supports manual refresh; and opens official store pages without account access.
- **Steam delisted-title safety**: launcher imports now lock metadata to the exact Steam app ID found locally. A missing public Store entry keeps the confirmed manifest title and identity instead of falling through to fuzzy matching.
- Friends correctly lives on the far right of the permanent sponsored rail; Coffee has a high-contrast style for light themes; Tools now uses a utilities icon.
- Theme cleanup: added Special **Generic Gray** and **Generic Blue**; Midnight is moonlit navy/starlight yellow; Industrial uses danger-yellow/neon-orange; Modern is slate-blue with restrained dark red; Daybreak is warm sunrise and Mint remains organic green.
- Home cleanup: compact Library Health blob, distinct **Top 5 played** and chronological Recent Sessions, top-five personal-rating-only My Best Games, decimal rating picker, green New Update motivation, and an expanded scrollable Chronicle.

## Completed locally — Genre Intelligence, Update Intelligence, and launcher adapters

- Canonical 20-genre taxonomy now normalizes core genre, subgenre, playstyle, perspective, and theme profiles separately from personal Library categories.
- Folder-scan Wizard review now previews that detected identity in a green **NEW** panel and persists the direct source tags when a game is accepted; it never creates or changes a Library category.
- Preview pane refinement: genre identity is now a dedicated theme-responsive vertical card; actions have been consolidated beside Launch and game facts are shown as a readable details list.
- Released This Week now uses a clearly-labelled semi-major fallback only if no strict major release qualifies, avoiding a blank Home pane without turning the feed into a release dump.
- The first normalizer is wired to metadata refreshes and local library hydration. It uses exact source-tag aliases (for example, Rogue-like → Roguelike) rather than loose description keyword matching.
- Preview presentation, conservative Auto-sort recommendations, and an approval-first batch review/enrichment flow for existing games are now in place.
- Metadata approval is now shared by refreshes and add-from-match: changed/fresh fields are green and marked **NEW** before the user accepts them.
- Tidy Up now runs a complete approval-first identity repair queue with repaired/skipped progress, a Stop Review control, and local title clues derived from the EXE, nearby folders, and strictly bounded README/title fields.
- Update Intelligence foundation now reads concrete pending Steam download bytes from local manifests and presents them in Home and Game Preview with a safe native Steam Downloads handoff. It performs no launcher writes and does not treat ambiguous state flags as updates.
- Independent-game update watch now accepts an installed version plus a user-chosen public source page in Customize. Explicit higher Version/Build/vX.Y labels surface in Home and Game Preview with a source link; NEO-LIB does not download or install files.
- Independent-game update alerts now open a read-only in-app patch-history timeline with detected dates, multiple explicit version entries, and “After yours” markers before offering the full source link.
- Native GOG import reads installed games from bounded Windows registry records, deduplicates by GOG ID/path, assigns a likely executable, and groups them under a real GOG category.
- EA App/legacy Origin is now a native local import adapter too, using bounded Windows installation records with product/version preservation, executable selection, deduplication, and EA category creation.
- Ubisoft Connect is now a native local import adapter using its installed-game registry, product identity and launch URI preservation, bounded executable discovery, deduplication, and Ubisoft category creation.
- Battle.net is now a native local import adapter based on verified Blizzard Windows installation records, excluding the launcher itself while preserving product/version identity and category grouping.
- Riot is now a native local import adapter based on bounded local product metadata, excluding the client while retaining product/version identity and configured game executables.
- Xbox/Game Pass is now a native local import adapter using bounded XboxGames roots and per-title MicrosoftGame.config data rather than broad Microsoft Store enumeration.
- Rockstar is now a native local import adapter using verified Windows installation records while excluding the launcher, Social Club, and support components.
- Launcher integration audit fixed the first-time detection path and Wizard so every supported adapter keeps its own executable, source, product/version identity, and category instead of falling back to the former Steam/Epic-only route.
- Launcher identity audit expanded the Library filter to all supported clients, corrected the Settings version display, and made Home prefer actual launcher ownership so metadata-only Steam app IDs cannot relabel local/repack games.
- Interaction audit replaced leftover double-click-only backdrops with consistent single-click-outside dismissal across the standard dialogs and custom metadata, Tidy, and changelog surfaces.

## Next up

- **FiFi installed acceptance** — the selector, articulated live rig, shared companion behavior and 27-line voice map are now implemented. Rebuild and visually/listen-test the packaged app before accepting timing, bounds and presentation.

- **Post-play rating prompt is already implemented** — sessions of at least fifteen minutes can request a one-decimal private rating, with seven-day snooze and per-game never-ask choices. The stale “queued next” note was removed during the missing-feature audit.
- Run the desktop bug-test pass, then build the Windows installer in a normal Windows/GitHub Actions environment.
- Native itch.io import now reads only the desktop app’s configured install locations and completed-install receipt markers. It deliberately leaves itch’s live `butler.db` catalog alone and sends folder-derived titles through the normal approval-first metadata flow. A real local itch fixture still needs desktop verification.

**Current state:** v1.7.3 is a recovery candidate, not release-ready. The reported startup bulk-launch regression now has a one-time native Launch-button authorization, a native click/key-intent gate, a fifteen-second startup quarantine, a shared cross-instance rate guard, a single-app-instance lock, a generic-link game-protocol block, and a local startup/allowed/blocked launch audit log. The boot overlay absorbs its own input. The experimental post-intro Fungist greeting has been removed from this recovery build pending a real Windows startup-and-single-launch test. It still needs desktop interaction testing and a normal Windows build before release. Discord presence/mirroring remains explicitly dropped.

## Planned research / platform-dependent work

- Official opt-in friend presence options per launcher.
- Launcher Adapter coverage for detection, import, launcher tags, updates, news, and safe native handoff.
- Non-Steam launcher update-state research where clients expose a reliable read-only local signal.
- itch.io catalog/database-level import remains intentionally out of scope: the implemented native importer is bounded to user-configured install locations and completed-install markers, never the live `butler.db` catalog.

## Known release checks

- Electron syntax, direct renderer bundle, Tailwind compilation, and whitespace checks pass.
- The normal Vite build cannot run in this workspace because its restricted process environment blocks esbuild spawning (`spawn EPERM`). This needs a normal Windows/GitHub Actions build before publishing an installer.
- A fresh Electron window also cannot be opened under this managed process sandbox: Chromium stops before app startup with a Windows platform-channel `Access is denied` error. Interaction tests therefore remain a real external validation gate rather than being marked as passed from static inspection.
- Packaging reaches electron-builder but its native `app-builder.exe` child process is blocked by the same environment (`spawn EPERM`). The checked-in Windows GitHub Actions workflow remains the normal packaging path after the changes are pushed.
