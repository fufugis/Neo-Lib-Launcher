# NEO-LIB active work queue

This is the authoritative live queue. New user requests are appended here and remain open until verified, explicitly cancelled, or blocked with a concrete reason.

## In progress

- [x] **Complete supported-launcher release coverage** — Recent Game Releases now includes the final missing imported launcher, itch.io, and permanently asserts the full Steam, Epic, EA, GOG, Ubisoft, Battle.net, Riot, Xbox, Rockstar and itch.io source set. Standalone entries remain on their existing per-game official news/update route because they have no common store catalogue. The focused fixture and complete pre-build source gate pass; rebuilt Windows/live-network acceptance remains required.

- [x] **All-launcher recent-release priority** — the Steam-dominated Released This Week feed is now Recent Game Releases, combining official publisher discovery across EA, Ubisoft, Battle.net, Xbox, Epic, Riot, Rockstar and GOG with verified Steam evidence. Major launches rank first and remain for 14 days; smaller verified releases expire after 5 days and are capped at four so catalogue volume cannot bury a major non-Steam title. Full pre-build source verification passes; rebuilt Windows/live-network acceptance remains required.

- [x] **Fourth Bright theme — Monochrome** — Theme Studio now offers a white, gray and black Bright option with its own high-contrast palette, geometric linework, grayscale atmosphere and restrained neutral particles. Library ambience, Effects intensity, motion cadence, Rest Mode and light-theme edge treatment all recognise it. Source verification passes; rebuilt Windows visual acceptance remains required.

- [x] **Library icon-only mode + Visuals placement** — Visual Tweaks now opens beside the resizable Library whenever the viewport has room. Its separate Icons only mode removes visible game names, category headers and the pinned strip while retaining selection, hover identification and right-click actions. Dedicated controls adjust 24–96px icons, 0–24px spacing and one-to-three compact lanes; incompatible standard text/category/layout controls visibly disable. Locked PIN-category games remain excluded through the existing privacy filter. Focused renderer-state and visual-boundary verification passes; rebuilt Windows visual/interaction acceptance remains required.

- [x] **Cover Wall personal-rating tags** — every currently visible/unlocked game with a personal rating now carries a yellow upper-right artwork tag showing its exact one-decimal score (`★ 4.5`). The tag scales its padding/text for Wall densities 3–10, stays above cover gradients and hover art, and is absent for unrated or still-locked games. Source verification passes; rebuilt Windows Wall review remains required.

- [x] **Mascot-only resizing boundary** — mascot size now changes only the FiFi/Fungist artwork, glow and immediate character effects. Speech bubbles, hover text, completion/launch messages, quick settings, chat and centre-screen major notices retain their full readable interface size. The layout reserves the mascot’s scaled footprint so a larger companion does not grow through its bubble. Source verification passes; rebuilt Windows checks at 70%, 100% and 145% remain required.

- [x] **Readable mascot notices + automatic external-game rest** — a confidently matched library game running through another launcher now enters Rest Mode immediately, pauses non-essential work and wakes automatically when that exact process closes. A large, calm, dismissible centre-screen mascot notice explains what happened for 7.5 seconds; major PC alerts share that surface. Ordinary mascot cards and voice bubbles are wider, less crowded and use larger text/actions. Focused renderer, watcher and visual-boundary checks pass; rebuilt Windows visual and live Valheim round-trip acceptance remains required.

- [x] **v1.7.7 GitHub release recovery** — the clean-tag run initially hit a transient NUL-filled ASAR fixture read after the same commit had already passed on `main`; the fixture passed ten consecutive local runs and the untouched GitHub retry completed in 2m 7s. GitHub produced the 311 MB `NEO-LIB-Windows` artifact and the public v1.7.7 release exposes six assets. The installed v1.7.7 app was opened directly on Windows: Home/news, FiFi and the foreground Control Center menu rendered and responded. Future releases now use the maintained Node 24 `softprops/action-gh-release@v3`, removing the final deprecated Node 20 action warning. Full clean/upgrade and feature-by-feature Windows acceptance remains open.

- [x] **GitHub EXE build portability repair** — the failed v1.7.7 run was traced to the visual-boundary verifier reading pnpm's private `node_modules/.pnpm` store even though GitHub installs dependencies with Yarn. The verifier now resolves Babel from Yarn/npm's normal hoisted location first and retains a pnpm fallback for local development. The complete pre-build suite passes locally. Artifact upload now uses the current Node 24 action runtime. The real renderer/package build still needs the unrestricted GitHub runner because this managed session blocks esbuild child-process creation with `spawn EPERM`. Release tags must use exact clean form `v1.7.7`; the pushed `v.1.7.7` tag is intentionally rejected by the update-safety gate.

- [x] **Visual polish batch + installed-app follow-up** — added four selectable in-app cursors (Windows default, Neon, Petal and Pixel), a visible 1.5-second wake transition after tray rest, draggable FiFi/Fungist quick settings with saved placement and mascot sizing, a compact Home game-update area that keeps its full text, and a lighter pink Anime theme. The installed screenshot exposed two misses: unanchored switch thumbs protruded beyond their tracks, and only the verified-update rows—not the full update body—were height-limited. Both are corrected and protected by the visual boundary gate. Rebuilt Windows visual acceptance remains required.

- [x] **GitHub Actions runtime maintenance** — checkout and Node setup use v5, while artifact upload uses v7, so GitHub no longer needs to force their deprecated Node 20 action runtime onto Node 24. A missing-artifact message is downstream of an earlier build failure, not an upload fault; protected feedback relay secrets must remain valid for a release build.

- [x] **Manual + tray Rest Mode** — added Rest Zzz beside Add/Wizard. It pauses the same non-essential work as game Rest Mode, changes to a glowing theme-accent Wake up action, and clearly confirms the transition. When close-to-tray hides NEO-LIB, native visibility events automatically enter Rest Mode; reopening wakes the app without altering game tracking. Source-verified; a rebuilt Windows tray round-trip remains required.

- [x] **App-wide typography comfort pass** — established one global hierarchy: compact labels and ordinary section titles that requested the old 900 weight now render at a calmer 700, while display-font heroes and 2XL+ numerical/title callouts retain an 800-weight presence. Existing ordinary bold text remains 700. No generic zoom or redundant preference was added because the baseline can be accepted first without risking layout clipping. Source verification passes; rebuilt Windows visual acceptance remains required.

- [x] **Settings ownership cleanup** — removed the stray “New category” action from Visual Tweaks; categories stay in Library management. Removed every old hidden Mascot chooser, visibility, voice, model and notification control from Settings, leaving Mascot Center as the single live owner. Settings now holds Sounds, onboarding, app behaviour, optional AI, build/about and support; added the standard preference to hide the sponsored game-deals strip. Static boundaries reject mascot/library-management duplication; rebuilt Windows review remains required.

- [x] **Wizard Reliability Pass — folder scan and metadata recovery** — manual Fast/Deep folder scans now use the same bounded operation model as launcher imports: Fast stops at 32 seconds, Deep at 55 seconds, each has Cancel scan, and late native results are ignored. Candidate metadata review is bounded to 28 seconds with Cancel lookup; optional image caching is limited to 12 seconds and can never block accepting a reviewed local entry. Failure/cancel/unavailable states return to a readable recovery path rather than leaving an endless spinner. Full source verification passes; installed Windows acceptance is still required.

- [x] **v1.7.7 regression guard — Control Center, hover bounds and Steam update truth** — source gates now permanently require the left Control Center toggle to retain a pointer-safe press path, delayed hover text to measure and remain inside the viewport, and Steam update cards to use the documented Downloads handoff while requiring live Steam update state in addition to byte counters. Full source verification passes; rebuilt Windows interaction remains required.

- [x] **Control Center portal delivery repair** — removed the invalid animation-wrapper path that passed the dropdown portal itself as an animation child. The actual menu now mounts directly in the foreground portal, while both the gear and panel stop Electron pointer and mouse bubbling before it can trigger outside-dismissal. Static checks reject restoring the fragile portal-through-animation shape. Rebuild and verify one real click in Windows.

- [x] **Control Center follow-up — readable menu and repeatable patch notes** — manual Patch notes now falls back to the current v1.7.7 entry after the automatic post-install notice has been acknowledged, instead of showing “You’re fully caught up.” The compact Control Center gear now matches the navigation tabs’ 36px height, and its menu labels/section headings use 700/600 weight rather than the harsher 900. Focused renderer, visual and operation tests pass; a rebuilt Windows check remains required.

- [x] **Discord feedback restoration** — Feedback once again has one normal destination: the protected signed Cloudflare relay which forwards to the configured Discord webhook. A locally built app without relay settings now states that Discord is not configured rather than silently opening GitHub. The webhook remains outside source and the packaged app; release builds still refuse to proceed without the relay URL/key. Offline relay security and operation checks pass; configure the existing Worker secrets and release build values before Windows acceptance.

- [x] **v1.7.7 release identity correction** — the installed v1.7.6 archive was a mixed package: it contained the Steam native fix but an older renderer without the Control Center and hover repairs. The unreleased candidate is now consistently identified as **1.7.7** across package metadata, About, updater tags, workflow, release/acceptance documents and the in-app patch-notes popup. Patch-note content is deliberately unchanged; a fresh Windows build/install is required.

- [x] **v1.7.7 acceptance-record precision pass** — updated the Windows acceptance record to match the verified 85-command native surface and added explicit hands-on checks for the Control Center click path, edge-clamped hover text, completed-vs-active Steam update evidence and safe Downloads failure behavior. This makes the remaining Windows handoff actionable without treating source checks as visual/runtime proof.

- [x] **Mascot event expansion — quiet by design** — expanded both Fungist and FiFi’s shared event pool with individually switchable launch send-offs, deliberate library additions, private-category protect/unlock/panic confirmations, and controller connect/disconnect notices while Controller Center is open. Existing PC health, favourite news/updates, app updates, completions and idle rest remain available. Events never expose private game/category names, controller IDs or input history; Rest Mode, hide/mute controls and voice cooldowns still stop reactions.

- [x] **External-game Rest Mode reliability repair** — replaced the fragile exact-EXE-only outside-launch check with an exact-file-first, install-folder-bounded fallback for launcher child processes. Wizard imports now preserve launcher/install evidence for future checks. Battle.net’s saved bootstrapper plus running `retail\\Overwatch.exe` is directly covered, while an idle Battle.net client outside that one game folder remains excluded. The check stays local, read-only and process-path-only; installed Windows acceptance is still required.

- [x] **Phase 1 follow-up — Mascot Center and controller shortcuts** — added a separate Mascot Center to the NEO-LIB Control Center for companion selection, visibility, voice preview/volume, chat model context and individually chosen observation notices. Mascot controls no longer appear visibly in Settings. Theme Studio now shows a clear disabled **Custom theme · coming soon** choice. Controller Center keeps the Windows Bluetooth/device route and adds Steam Input, which only opens Steam Big Picture after a native running-Steam check.

- [x] **Phase 1 — NEO-LIB control menu and first-class Controllers** — moved the gear to the far left of Home/Library/Wall/Tools and made it the foreground-safe Control Center. Themes now has its own focused studio; Visual Tweaks, Settings, Controllers, patch notes, update checks, feedback and a true Quit NEO-LIB action are grouped in one place. The former broad Theme picker and Controller section were removed from Settings. TV Mode is visible but honestly marked Phase 2; Lite Mode remains Phase 3. Renderer, visual-boundary and all 85 native-command contract checks pass.

- [x] **v1.7.7 complete patch-note pass** — added one structured GitHub Release document covering the architecture rebuild and player-visible changes, connected the Windows release workflow to that exact body, linked both public READMEs to it, and replaced the 118-item in-app wall of text with a curated overview plus focused fixes. Historical development detail remains archived in source; the notes continue to say Windows acceptance is pending rather than calling the candidate published.

- [x] **v1.7.7 release-pipeline identity, feedback and credential gate** — GitHub Actions now refuses missing/malformed feedback settings instead of silently shipping a broken Feedback button, verifies the configured relay values exist in the packaged renderer, scans source/package output for known credential signatures and keeps `.env` outside the archive. Both GitHub and the guarded local release driver remove the temporary plaintext renderer `.env` in a `finally` path even if compilation fails. An offline relay fixture proves CORS, signing, replay/UTF-8 body limits, per-IP throttling, mention suppression, unavailable-service responses and fixed capped Discord payloads without contacting the network. If that configured relay is temporarily unavailable, the Feedback window keeps the typed report and offers a deliberate GitHub fallback. Optional Discord Rich Presence is generated before the source fingerprint, restricted to a numeric App ID, kept outside tracked source and verified inside the package. The candidate inspector records only non-secret enabled/disabled facts plus installer/portable/archive hashes and emits a public checksum file without local paths. This closes the previous after-render mutation that would have made provenance stale and official revisions appear dirty.

- [x] **v1.7.7 package dependency cleanup** — removed the unused `electron-store` production dependency after a repository-wide reference audit confirmed the app now uses its tested native document/storage owners exclusively. The release gate keeps the production dependency set empty until a real native runtime import is deliberately added, avoiding dead packaged code and its transitive dependency tree.

- [x] **v1.7.7 legacy update-discovery contract** — confirmed that installed v1.7.3 checks GitHub's latest published Release on launch, reproduced why the historical dotted `v.1.7.5` tag is not recognized by that old parser, and reserved the current architecture build as v1.7.7. The current checker now tolerates both dotted and clean tags; a build-blocking fixture proves old v1.7.3 recognizes exact clean tag `v1.7.7`; and the GitHub Windows release workflow refuses any tag that does not exactly match `v<package version>`. Publishing the verified installer as a non-draft GitHub Release under `v1.7.7` remains a release action, not a source claim.

- [x] **Architecture Stage 9B — renderer composition cleanup, batch 1** — removed another 127 lines of product rules from `App.jsx` without changing visible behavior. Hardware-tool defaults/idempotent merge, launcher helper-EXE repair, the privacy-bounded mascot library context, all ten launcher labels/categories, update-ledger bounding and mascot-inbox bounding now have focused owners and direct fixtures. The actual ten-launcher Wizard import path, renderer bindings, visual boundaries and release-hardening gate pass after the move.

- [x] **Architecture Stage 9B — Library Visuals boundary complete** — moved the complete Visuals popover, its three control lanes, sliders, font choices, category controls, FX/motion controls, texture picker and feedback shortcuts out of `Sidebar.jsx` into `components/library/LibraryVisualsPopover.jsx`, with font policy in `library-visual-model.mjs`. `Sidebar.jsx` fell from about 2,320 to 1,796 lines and now only opens the panel and supplies state/actions. A dedicated regression boundary preserves every control/test hook, foreground layering, the five font choices and fallback behavior; renderer bindings/state and the full source gate pass.

- [x] **Architecture Stage 9B — Preview information and hero boundaries complete** — moved About/Identity, verified media, factual details, hero title and precise personal rating out of `GameDetail.jsx` into focused `components/preview/` owners. The central Preview component fell from roughly 1,140 to 918 source lines while preserving description boundaries, the eight-image cap/deduplication, category-independent genre identity, empty states, release/title rendering and tenth-step rating behavior. Direct model fixtures, permanent component-boundary assertions, all 96 renderer bindings and the full source gate pass.

- [x] **Architecture Stage 9B — Preview actions boundary complete** — after removing three unreachable legacy renderers, moved the complete live toolbar and managed-tool menu to `preview/PreviewActionBar.jsx`. Launch, YouTube, patch notes, mods, Customize, reviewed refresh, folder, saves, categories and the foreground category popup retain their original wiring. `GameDetail.jsx` fell from 737 to 517 lines. The launch gate now follows the dedicated owner and still proves the single visible launch control requests native one-use authorization; visual hooks, popup ownership, all 97 renderer bindings and the full source gate pass.

- [x] **Architecture Stage 9B — Preview status boundary complete** — moved Steam manifest evidence, all-launcher live news, managed-tool readiness and update-available/history presentation into `preview/PreviewStatusCards.jsx`, with age/size/version/remaining calculations in `preview-status-model.mjs`. All four native calls, cancellation guards, news expansion, external links, truthful attention/error text and patch-history review remain intact. `GameDetail.jsx` fell from 517 to 216 lines and is now a focused composition component. Direct calculation fixtures, all status hooks, 99 renderer bindings and the full source gate pass.

- [x] **Architecture Stage 9B — Sidebar live tree boundary complete** — moved two-column balancing, category sections, game rows, pinned games and both foreground context-menu systems into `components/library/LibraryTree.jsx`, with the column split policy in `library-tree-model.mjs`. Drag/drop, locked-category privacy, selection, badges, playtime/source labels and all existing test hooks remain intact. Direct split fixtures prove categories are neither divided nor duplicated; permanent boundary checks and all 102 renderer bindings pass.

- [x] **Architecture Stage 9B — Sidebar toolbar-control boundary complete** — moved the shared action button, decorated main tab and launcher dropdown into `components/library/LibraryToolbarControls.jsx`; removed the unreachable old launcher-pill renderer. Opaque navigation chrome, theme artwork, news badge animation, click-outside/Escape behavior and every launcher option remain unchanged and source-guarded. Together with the tree move, `Sidebar.jsx` fell from roughly 1,847 to 798 lines.

- [x] **Architecture Stage 9B — App modal and overlay composition complete** — moved every dialog, metadata review flow, Settings recovery wrapper, Controller Center bridge, tutorial, intro, feedback/playtime import, drag overlay, toast and launch/update troubleshooting surface into `components/app/AppModalLayer.jsx`. `App.jsx` retains the state and workflows and supplies an explicit context contract; it no longer imports or renders the individual modal implementations. The Controller Center ownership test follows the new boundary, all modal hooks stay source-guarded and all 104 renderer bindings pass.

- [x] **Architecture Stage 9B — browser-preview seed boundary complete** — moved the three demo games, private demo category and three demo tools out of `App.jsx` into the pure `state/demo-library.mjs` factory. A direct fixture proves deterministic timestamps and injected PIN hashing without reading or writing a real library.

- [x] **Architecture Stage 9B — metadata workflow controller complete** — moved per-game refresh, reviewed candidates, exact Steam identity locking, Battle.net product handling, cached artwork application, accept-before-write, Tidy repair queues and confirmed bulk refresh into `services/metadata-workflow.mjs`. `App.jsx` retains state and injects UI/native dependencies through an explicit factory contract. Direct injected-native fixtures prove installed-app refusal, no black-box replacement, Steam/Battle.net identity behavior, queue startup and bulk confirmation. `App.jsx` fell again from roughly 2,030 to 1,860 lines and all 105 renderer bindings pass.

- [x] **Architecture Stage 9B — category/privacy workflow complete** — moved category creation/edit/delete/reorder, assignment-only removal, game drag/move/reorder, PIN setup/removal/unlock and panic locking into `services/category-privacy-workflow.mjs`. The workflow injects state/UI actions while reusing the tested library/privacy transitions. Pure fixtures prove wrong/right PIN behavior, private-game protection during bulk regular-category removal, safe panic selection and drag movement. `App.jsx` fell from roughly 1,860 to 1,755 lines and all 106 renderer bindings pass.

- [x] **Architecture Stage 9B — launcher import/detection workflow audit complete** — extracted running-client eligibility into `launcher-detection-workflow.mjs` and removed the unreachable 100-line silent-import branch plus its retired `launcherAutoImport` dependency. Detection now has one explicit promise: offer the confirmation-first Wizard only; it cannot scan, import, repair paths or refresh metadata merely because a client is running. Fixtures cover existing libraries, dismissal, 24-hour postponement, all ten Wizard imports, duplicates, category creation, timeout/rejection recovery and cancellation. `App.jsx` fell from roughly 1,755 to 1,640 lines.

- [x] **Architecture Stage 9B — final root workflow/dead-code audit complete; renderer architecture frozen** — moved reviewed Auto-sort apply/exact undo into a directly tested workflow owner; removed the unused legacy `importMany` bulk path, unused `ensureOrder` helper and two impossible-to-change refresh-progress states/props. A permanent binding gate now rejects unused imports or root `App` declarations as well as crash-causing unbound names. `App.jsx` is roughly 1,590 lines, with remaining code limited to composition, transient screen state and cross-feature wiring. No further renderer movement is justified before installed acceptance.

- [ ] **Architecture Stage 9 — installed acceptance and performance pass (active; external build gate)** — the complete v1.7.7 source suite passes, including 181 runtime files, 113 renderer modules, 85 native commands, launcher/metadata/privacy/FiFi/operation checks and the clean-tag update contract. The real Vite build was attempted immediately afterward but this managed environment again refused esbuild process creation with `spawn EPERM` before compilation. The packaging guard independently rejects the September renderer as stale for the then-current source fingerprint, and continues recalculating after every change, so an old interface cannot be repackaged accidentally. Next: run the build in an unrestricted Windows terminal, then verify clean and copied-upgrade profiles, startup/restart/tray, operations, launch/low-use recovery, launcher Wizard, privacy and visual/audio behavior, and record same-machine performance. Publish only after acceptance passes, using exact clean tag `v1.7.7`.

- [ ] **Controller/large-screen track deliberately parked** — per the player’s direction, keep C2 integration, Minimalistic C3 and NEO Lounge C4–C5 queued but do not advance them during the current architecture pass.

- [x] **Controller / experience C0 foundation** — documented the independent Default/Minimalistic interface choice and Desktop/NEO Lounge presentation choice so themes and feature logic remain shared. Added pure controller inventory normalization, a standard semantic action map, connection-difference model, no-background-polling browser adapter and the official Windows Bluetooth settings hand-off route. No visible mode or controller interception is active yet; focused source tests pass.

- [x] **Future milestone inventory synchronized** — `FUTURE_MILESTONES.md` now sequences current architecture acceptance, Controller Center, shared navigation, Minimalistic, NEO Lounge, optional connected intelligence and final release work. The audit also corrected stale notes: post-play ratings already exist, FiFi is now live in source, and the local-day playtime bug is repaired.

- [x] **Controller Center C1 — source complete** — Settings now opens a real Controller Center that refreshes only while visible, lists browser-visible connected gamepads, distinguishes identical pads, remembers only a short preferred-device fingerprint and shows live button/stick activity without recording input history. Pairing/removal hands off to an exact allowlisted Windows Bluetooth route; unsupported battery, wireless-strength and disconnect data is stated honestly. Focused renderer, native-contract and visual-boundary checks pass.

- [ ] **Controller Center C1 — installed acceptance** — in a rebuilt Windows app, open Settings → Controller Center with no pad, one pad and (if available) two similar pads; verify connect/disconnect, preferred selection persistence, live buttons/axes, close-time polling stop and the Windows Bluetooth hand-off. Battery/disconnect controls must remain absent rather than guessed.

- [x] **Controller navigation C2 safety foundation** — semantic standard-mapping commands, stick dead zones, edge-only activation, delayed directional repeat, duplicate-start prevention, text-entry suppression, modal HOME suppression and an explicit-lifecycle animation-frame adapter are source-tested. The dormant activation policy refuses controller activation of Launch controls so it cannot bypass the current trusted pointer/keyboard launch boundary.

- [ ] **Controller navigation C2 integration** — add one opt-in focus owner for existing UI, with visible focus, surface/modal traps, reconnection, private-lock and panic-lock behavior. Design a dedicated hold-to-launch confirmation that preserves native launch safety before controller Launch is enabled. Do not emulate controller input inside games.

- [ ] **Minimalistic interface C3** — introduce a reversible presentation policy over the same actions/data: calmer hierarchy, progressive disclosure and fewer simultaneous controls while preserving themes, advanced features, privacy and launch safety.

- [ ] **NEO Lounge fullscreen C4–C5** — build an explicit opt-in controller-first distance UI after controller navigation is stable, then add NEO-LIB-specific polish around sessions, updates, private shielding, mascot guidance and low-use launch. Never force fullscreen at startup.

- [x] **CPU usage alignment with Windows — source complete** — the installed app showed 49% while Windows showed 19%. NEO-LIB was averaging CPU time across the entire 15-second polling gap after its first read, while Task Manager normally presents a much newer view. Every read now takes its own fresh one-second whole-machine sample; repeated-read fixtures prove an old interval cannot be reused. The footer labels the sampling window and honestly notes that momentary differences remain possible.

- [ ] **CPU usage alignment — installed comparison** — after the next rebuild, place NEO-LIB beside Task Manager on Normal update speed, manually refresh NEO-LIB three times under idle and moderate load, and record both values. They need not be identical at every instant, but should track the same direction and no longer show a stale 15-second average.

- [ ] **Earlier-request installed acceptance sweep before architecture close** — inspect the already-queued user requests that have source checks but no installed confirmation: launcher Wizard/category creation; Special navigation/button artwork and resizing; Preview transparency; global popup layering/readability; refresh candidate picker; private-category/Home/Wall behavior; mascot dock/voice/startup; Home grouping; theme readability/FX; auto-sort; and normal/reduced-width visuals. Record each as accepted, needs correction or deliberately deferred. Optional product decisions such as mascot web research or third-party metadata credentials do not block architecture completion.

- [x] **Architecture diagnostics recorder — source complete** — replaced raw startup/renderer failure text and launch-detail logging with one rotating, local, structured recorder. It captures startup, renderer load/crash/unresponsive, Electron child-process, main-process and IPC failure classes plus bounded status facts, while dropping game names, paths, URLs, searches, payloads, message text, keys, PINs and private-category identity. Feedback can copy the report or open its folder, and includes it only through an unchecked player-approved option. Rotation/privacy, all 83 IPC mappings, renderer bindings, visual boundaries and release hardening pass; rebuilt Windows crash/copy/folder acceptance remains open.

- [x] **Architecture Stage 4 — provider/update source implementation complete** — seventeen dependency-injected provider and evidence services now own store metadata, public/specialist discovery, candidate dispatch, Gemini, news, weekly releases, owned feeds, update history, deals and independent update assessment. The final assessment boundary preserves four-game batching, 9-second bounded page requests, a fifteen-minute evidence cache and honest `needs-evidence` results; cached missing evidence can no longer become a false `current` result. The source audit proves one route per feed, no legacy provider caches in `main.js`, no hidden direct process/Electron/network dependencies inside providers and no automatic metadata apply. Offline regression and a bounded live smoke across Steam, GOG, public search, itch.io, Steam News and four deal sources pass. Packaged Windows refresh acceptance remains blocked by the managed environment's `esbuild spawn EPERM`, not by a failed source test.

- [x] **Architecture Stage 6 — renderer-state source implementation complete** — Library/categories, navigation/selection, private-game redaction and panic locking, metadata review queues, playtime, settings/visual derivation and persistence now have separate tested owners. `App.jsx` consumes one stable preload gateway instead of 69 direct `window.api` references, Home stays the startup view while last safe Library/Tools selections survive restart, and locked Home/Wall data is globally tested against identity/art/path/source leakage. Theme ambience and decorative rendering moved into `ThemeVisuals.jsx` without changing the artwork. The new gate parses JSX and exercises pure transitions; the complete regression suite passes. Packaged Home/Library/Wall/Tools navigation and visual acceptance remain blocked by the managed environment's `esbuild spawn EPERM`.

- [x] **Architecture Stage 7 — component/visual source boundary foundation complete** — release content is separate from changelog behavior; Settings controls, Home calculations and Fungist command/notice decisions have focused owners; and all portal-based menus/modals now share one foreground gateway. Theme decoration is explicitly pointer-safe. The new gate parses every renderer component, refuses extra portal owners or clickable decoration, and tests private Home redaction plus Home/Fungist decisions. The complete offline regression suite passes.

- [ ] **Architecture Stage 7 — packaged visual acceptance** — verify popup order, keyboard/focus, narrow-window bounds, reduced motion, every theme, readable surfaces and private redaction at standard and reduced widths. Vite currently stops before bundling because the managed process sandbox refuses esbuild with `spawn EPERM`; this is an external acceptance gate, not a failed source test.

- [x] **Architecture Stage 8 — truthful operation lifecycle source implementation complete** — launcher scan/import, metadata review, Home/news refresh, weekly releases, game updates and storage scans now share bounded operation IDs and explicit running/succeeded/partial/failed/cancelled/timed-out/unavailable states. Active player-requested work exposes Cancel; superseded or closed-window results cannot later overwrite current state. A forty-entry local diagnostic journal stores only domain/status/timing/counts/error code. Feedback includes it only through an unchecked player-approved option, and tests prohibit game names, paths, searches, keys, message text and private identity.

- [ ] **Architecture Stage 8 — packaged operation and diagnostic acceptance** — in a rebuilt Windows app, cancel one launcher scan, time out an injected test operation, confirm Home/News recover without a spinner, reproduce one safe test error, copy the diagnostic report, open its folder, and inspect an explicitly included feedback summary for private-data exclusion. The managed environment's `esbuild spawn EPERM` prevents that packaged check here.

- [x] **Architecture Stage 9A — consolidation and release source gate** — removed one shadowed `stripHtml` definition, eight proven-unused metadata/Gemini/news helpers and one deprecated no-output visual stub, reducing `electron/main.js` by 101 lines without changing its active service routes. A dependency-free release gate checks every runtime file for duplicate top-level declarations and unresolved relative modules, aligns package/renderer/changelog versions, verifies package entries and assets, refuses retired helpers and requires a final ownership map. Release acceptance and rollback instructions are documented.

- [ ] **Architecture Stage 9 — packaged release and performance acceptance** — build installer plus portable app in unrestricted Windows, run clean-profile and copied-upgrade smoke tests, exercise cancellation/launch/low-use recovery and complete the visual acceptance queue. Record cold start, five-minute idle CPU/RAM, representative Library rendering and before/during/after low-use measurements against the previous known-good build on the same machine. Until those facts exist, Stage 9 remains source-hardened rather than release-ready.

- [x] **ASAP build identity and stale-interface lockout** — every Vite renderer now embeds a deterministic fingerprint of the exact native, renderer and public runtime source plus build time, Git revision and the current `Stage 9B source-frozen` label. Settings → About displays the short ID. Electron Builder's `beforePack` refuses missing or stale `dist-renderer` output even when invoked directly from Visual Studio. Tests prove stable fingerprints, source-change invalidation and safe packaging refusal; older September renderers are correctly rejected.

- [x] **ASAP complete rebuild and package inspection** — the unrestricted Windows rebuild produced a new 147,117,754-byte installer and 64,589,421-byte `app.asar`. Internal inspection proves renderer fingerprint `3EED3299FF39`, the Stage 9A label, Stage 8/9 and build-identity content, Settings build information, 24 FiFi rig/studio files, 29 FiFi future-voice files and an exact packaged/current `electron/main.js` match with one `stripHtml` owner. Installer SHA-256: `F1C4433294D93073BAE28632ECB40D4C71F688484812B58EE8A3AEA9AE486E46`.

- [x] **ASAP blank-window startup source repair** — the installed coherent build exposed a real renderer crash: `notificationEnabled` had been extracted into Fungist's model module but was not imported by `FungistMascot.jsx`. Because the failure occurred in an immediate effect, React removed the interface before intro/Library became usable. The import is restored; the one other unbound renderer name found during the audit was repaired; all 79 renderer modules now pass a new build-blocking unbound-name gate. Startup also renders a safe recovery screen and records bounded local renderer load/console/process diagnostics instead of failing invisibly.

- [ ] **ASAP rebuilt startup acceptance** — **core launch is now confirmed by the user** after rebuilding v1.7.5 from the repaired source. Still verify intro completion, Home, Fungist, Settings → About build identity, restart and close-to-tray return before closing this acceptance item.

- [x] **ASAP FiFi live integration — source complete** — FiFi is now selectable beside Fungist and uses the shared companion host for alerts, AI chat, dragging/saved docking, flight/launch/completion reactions, voice controls, reduced motion and Rest Mode. Her articulated rig supplies blink, gaze, breathing, fins, tendrils and event particles. All 27 separate recordings are mapped exactly once to the existing bounded voice events, Settings previews the selected mascot's pool, and dedicated FiFi plus renderer-binding checks pass.

- [ ] **ASAP FiFi installed visual/listening acceptance** — rebuild and select FiFi in Settings; verify her rig loads in the packaged app, drag persistence, hover/chat/notice bounds, launch flight/home return, reduced motion and Rest Mode. Listen to all 27 lines for wrong files, clipping, unsuitable timing or mismatched reactions. Source mapping is complete, but those subjective checks cannot honestly be inferred from static tests.

- [ ] **ASAP acceptance, commit and push** — after the rebuilt Windows app passes the architecture, visual, clean/upgrade and performance checklist, create a reviewed commit containing the current working tree and push it to the configured Git remote. Do not publish the stale mixed installer or describe the restructure as release-ready before acceptance.

- [ ] **Architecture Stage 3 — contracts and safe failure logging complete; acceptance work remains** — all 85 native commands use one duplicate-safe registry and 28 explicit domain modules, with every preload invoke mapped exactly once and zero direct registrations in `main.js`. All 53 payload commands reject malformed or oversized renderer data before native work; the other 32 intentionally take no payload. All 85 commands validate native results, preserve valid success/failure results, and convert malformed responses or thrown/rejected service failures into contract-safe fallbacks. Channel-aware local diagnostics retain only the failure type and error class, never payloads or error text. Packaged Windows acceptance remains open.

- [x] **Architecture Stage 3 save-service extraction + copy repair** — moved all six inspect/list/backup/restore/auto-detect/search implementations out of `main.js` into one dependency-injected save service without changing IPC names or response contracts. A temporary-disk regression test exposed that the previous implementation created a copy destination and simultaneously required it not to exist; backup and restore now copy children individually with no-overwrite semantics. Tests cover metadata, confinement, occupied-folder refusal and bounded searching. Packaged Windows interaction acceptance remains part of the open Stage 3 gate.

- [x] **Architecture Stage 3 storage-scan extraction** — moved Home's game/mod folder measurement and cache out of `main.js` into a dependency-injected storage service. Temporary-filesystem tests directly verify missing and launcher-target refusal, duplicate/shared-root protection, mod totals, cached results and force refresh. The command remains explicitly user-triggered and retains its three-folder concurrency limit and existing IPC contract. Packaged Windows interaction acceptance remains open.

- [x] **Architecture Stage 3 Optimize junk-service extraction** — moved bounded junk discovery, scan-token snapshots and Recycle Bin dispatch out of `main.js`. Temporary-filesystem tests prove configured game/save paths remain protected, fresh files and undersized archives stay excluded, duplicate tokens collapse, changed/stale files are refused, and only the injected non-force Recycle Bin action runs. No test deletes user data. Optimize process inspection remains the next extraction; packaged Windows interaction acceptance remains open.

- [x] **Architecture Stage 3 Optimize process-service extraction** — moved Windows performance-result normalization, protected-process classification, fresh snapshot ownership and cooperative close dispatch out of `main.js`. Tests prove Windows-critical and NEO-LIB processes, changed names and stale snapshots cannot be closed; the only permitted command is exact-PID `taskkill` without `/F`, with Windows refusal reported safely. No process was started or stopped. Packaged Windows acceptance remains open.

- [x] **Architecture Stage 3 launch/process-watch extraction** — moved one-time authorization, startup quarantine, local/shared cooldowns, exact process start, running-game lifecycle and exit reporting into `game-launch-service.cjs`; moved path-only external-game detection, helper filtering, locally launched exclusion, state-change delivery and 8s/30s resume cadence into `external-game-watch-service.cjs`. Direct tests exercise every safety branch without launching, querying or stopping a real process; the full regression gate passes. Packaged Windows launch/resume acceptance remains open.

- [x] **Architecture Stage 4A Steam/GOG store-provider extraction** — Steam search/details and GOG search now delegate to a dependency-injected provider service without changing IPC names, URLs, result shapes, taxonomy evidence, screenshot limits or error fallbacks. Its isolated contract test uses fake HTTP only and proves empty/offline behavior without touching the network. The complete regression gate passes; packaged Windows and live-provider acceptance remain open.

- [x] **Architecture Stage 4B shared public-web provider extraction** — DuckDuckGo and Google HTML parsing, raw software/news searches, game-specific query wording, DuckDuckGo-first fallback order and conservative synthesized metadata now have one dependency-injected service. Offline fixtures prove URL encoding, the eight-result boundary, title/snippet cleanup, genre/year synthesis and empty/network-error behavior without a live request. Existing tools and news callers retain raw queries; game metadata retains its specialized suffixes.

- [x] **Architecture Stage 4C specialist metadata-provider extraction** — itch.io search/page details, DLsite code detection/product lookup, VNDB API mapping and Ryuugames fallback now delegate to one injected provider service. Offline fixtures preserve parsing, creator/developer mapping, six-image caps, Visual Novel identity and empty/network-error fallbacks. No source order or live-network behavior changed.

- [x] **Architecture Stage 4D metadata-candidate orchestration** — list/expand dispatch for Steam, GOG, itch.io, DLsite, VNDB, Ryuugames, F95Zone, public web and Gemini now uses one explicit allow-listed service. It centralizes normalized queries plus empty, unknown, malformed and thrown-provider fallbacks while preserving the existing source helpers, AI context and user-controlled candidate approval flow. Offline tests invoke no provider.

- [x] **Architecture Stage 4E Gemini provider adapter** — model allow-listing, game-metadata requests/normalization and Fungist assistant requests now delegate to an injected Gemini adapter. Tests preserve API-key/query errors, metadata caps, 12-message compact history, 24,000-character sanitized visible-library context, response settings and existing opt-in calls through fake HTTP only. No prompt, key or library data was sent online.

- [x] **Architecture Stage 4F news normalization boundary** — language filtering, safe public-result URLs, relative/absolute news dates, game-name confidence tokens and official-site classification now have one deterministic service with injected time. Offline fixtures preserve the existing rules and explicitly avoid changing the separately queued timezone behavior.

- [x] **Architecture Stage 4G public-news provider** — official-site-first query building, DuckDuckGo-to-Google fallback, confidence filtering, duplicate preference, two-item limit, six-hour cache and force refresh now live behind one injected provider. `news:fetchAll` and latest-game news retain their existing feed priority and use the provider-owned cache.

- [x] **Architecture Stage 4H weekly-release provider route** — SteamSpy plus Steam New Releases candidate merging, seven-day store verification, four-request concurrency, major/noteworthy/popular fallback tiers, twelve-item cap, six-hour cache and force/offline results now run through an injected provider with fixed-time fixtures. The previous body is temporarily retained unreachable in `main.js` for a dedicated consolidation deletion pass; packaged/live acceptance remains open.

- [x] **Architecture Stage 4I weekly-release consolidation** — removed the unreachable pre-provider weekly implementation plus its private cache/date/concurrency helpers after the provider equivalence suite and full mocked `main.js` boot passed. `releases:weekly` now has exactly one implementation route.

- [x] **Architecture Stage 4J Steam owned-news provider** — Steam News batching, 30-minute cache, BBCode/HTML cleanup, article-image extraction, English/date filtering, per-game failure isolation and newest-first ordering now live in an injected service. Offline fixtures retain the eight-game batch and existing API request shape.

- [x] **Architecture Stage 4K itch.io/GOG owned-news provider** — itch devlog RSS and GOG changelog fetching/parsing now share one injected owned-feed service. Tests preserve source URLs, HTML cleanup, image priority, date cutoffs, item identity and independent offline failure without changing the unified feed order.

- [x] **Architecture Stage 4L update-history provider** — user-requested public update pages now delegate to an injected service that refuses non-HTTP(S) URLs, bounds downloaded-text processing, discovers/deduplicates/sorts versions, extracts nearby dates and summaries, limits output to twenty and compares against the installed version. Offline fixtures make no request.

- [x] **Architecture Stage 4M deals provider** — Epic free games, Steam specials, Instant Gaming, GOG, Fanatical and Ubisoft discovery now delegate to one dependency-injected service. Offline fixtures preserve source URLs, thresholds, result shapes, source caps, deduplication, timeout arguments, independent failure containment and the populated-results-only one-hour cache. No live store was contacted.

- [x] **Architecture Stage 4N update-scan coordination** — exact-input cache keys, the 15-second recent-result cache and same/different-key in-flight serialization now live in a dependency-injected coordinator. Deferred fake scans prove order-insensitive keys, cache/force/expiry behavior, overlap control and rejection recovery without inspecting a live library; update evidence collection remains unchanged.

- [x] **Architecture Stage 4O update-source discovery** — saved/official pages, public Steam notes, maintained Battle.net product pages and carefully matched public-search candidates now share one dependency-injected discovery service. Fixed-time fixtures preserve URL normalization/deduplication, source confidence, title/update matching, three-source cap, DuckDuckGo redirect cleanup, the six-hour cache and best-effort failure without a live search.

- [x] **Architecture Stage 4P installed-version evidence** — bounded local version-file and Windows resource interpretation now delegates to one dependency-injected service. Temporary fixtures preserve absolute-path refusal, two-nearby-folder scope, the 24-file cap, Blizzard `.build.info`, Unity and labelled config evidence, filename fallback, 30-second launch quarantine and weak encoded-PowerShell resource evidence. The game path remains inert environment data; no real game was inspected or executed.

- [x] **Architecture Stage 4Q update-page version evaluation** — numeric version comparison, bounded Version/Build/v discovery, trusted-source preference and latest-evidence selection now use one pure injected service. Text fixtures preserve the 80-match cap, confidence tie-breaking and search-only fallback without reading a page or library.

- [x] **Architecture Stage 4R independent-update assessment** — per-game source fetching, fifteen-minute evidence caching and available/current/needs-evidence result construction now live in one injected service. Four-game orchestration, 9-second source timeouts, the 500,000-character evidence boundary and weak/missing evidence states are preserved. A regression fixture proves cached pages with no explicit version remain `needs-evidence` instead of silently becoming `current`.

- [x] **Installed-version prerelease suffix correction** — reordered the bounded installed-version patterns after extraction so full `alpha`, `beta` and `rc` suffixes are retained instead of truncating `rc1` to `r`. Dedicated JSON, Unity, filename, text and Windows-resource fixtures pass.

- [x] **Playtime range timezone repair — source complete** — daily snapshots, 400-day pruning and weekly/monthly/yearly cutoffs now use the player's local calendar date rather than converting local midnight back to UTC. A positive-offset midnight fixture proves January 2 stays January 2 and range deltas retain the correct baseline. Stats installed acceptance remains part of the broader Windows pass.

- [ ] **Architecture Stage 2 — storage code verified; packaged migration acceptance pending** — introduced a low-level app-data boundary plus a separate versioned document store for library, settings and playtime. Legacy files receive schema 1 only after an exact `.bak`; valid saves serialize and retain last-known-good; corrupt primary recovery, unsupported-version protection, invalid-save refusal, migration failure, window-settings merging, private/unknown fields and playtime all pass virtual-disk tests. All existing regression gates pass. Build re-attempted and remains blocked before bundling by `spawn EPERM`; do not call Stage 2 complete until disposable AppData migration, restart, edit, privacy, playtime and rollback are checked in the packaged Windows app.

- [ ] **Architecture roadmap — source restructuring complete, acceptance remains** — `ARCHITECTURE_ROADMAP.md` now records completed source boundaries for safety/build identity, launchers, persistence, IPC, providers, launch/process/power saving, renderer state, interface/visual ownership, diagnostics and consolidation. Stop adding structural movement. Close the roadmap only after the clean/upgrade, operation, launch/low-use, earlier-request visual/privacy and performance acceptance gates pass in the rebuilt Windows app.

- [ ] **Architecture step 1 — code extraction verified; build/desktop acceptance pending** — moved ten native launcher scanners into `desktop-app/electron/launchers/scanners.cjs`, retaining existing IPC and shared Steam/Blizzard helpers. Forty before/after fixtures and ten full-main mocked integration scans match results and I/O traces; the current 85 IPC endpoints register. Existing import, refresh and launch-safety checks pass. Launcher tests now gate renderer builds. Build attempted but blocked by `spawn EPERM` starting the build helper; no packaged-app smoke test completed. No detection fixes, UI/launch/storage changes, migration, real library writes, commit or release.

- [x] **FiFi per-voice performance script** — mapped all 27 exact filenames to triggers, opening/emphasis/settling poses, optional nod/bow/presentation gestures and personal particles. The live selector now uses this separate pool; mapping, replacement, cancellation and lifecycle checks pass. Subjective listening/word alignment remains in installed acceptance.
- [ ] **FiFi reaction particle visual acceptance** — nine restrained styles implemented with six reusable particles, FX scaling and rest/reduced-motion suppression. Source/controller checks pass; browser rendering remains unverified because local preview access was blocked by browser URL policy.

- [x] **Store and connect FiFi voice pack** — all 27 user-supplied recordings remain separate from Fungist under `voice-packs/fifi-future` and are now selected only when FiFi is active. Existing Fungist files and behavior remain intact.

- [ ] **Remove repeated category launcher names** — shared category header now suppresses duplicate white launcher wording when the badge already names it, including launcher aliases. Custom names, locked-category Unlock wording, arrows and counts remain intact. Alias/custom-name and component checks pass; installed visual acceptance pending.

- [ ] **Launcher wizard hang/category repair** — found confirmation rendered outside the portal used by the parent wizard, plus sequential unbounded metadata/image waits before onAccept. Confirmation now portals to body with the underlying wizard hidden; imports persist local entries/categories without network metadata waits. Added scan timeout, Cancel and stale-run checks, synchronous duplicate-click guard, and correct separation of other launcher product IDs from Steam appid. Actual wizard import/App category functions pass mocked tests for all ten launchers, duplicates and existing-game preservation; seven launch-safety checks pass. Live EA/Epic discovery and packaged UI verification pending. Import metadata is now obtained afterwards through Refresh info.

- [ ] **Small Preview transparency adjustment** — reduced only the main Preview backdrop opacity from 62% to 56%. Kept inner reading-card fills, text colors, blur, hero and menu surfaces unchanged to protect readability. Source validation complete; installed visual acceptance pending.

- [ ] **Magical four-corner refinement** — regenerated delicate wisp/star corner sprite, removed old cropped chunky ornaments, mirrored full sprite into all four corners. Existing magical-button-frame-v2 border lines unchanged. Corner width shrinks on narrow buttons; alpha, four-corner count, old-asset exclusion and preview CSS/component checks pass. Visual acceptance pending.

- [ ] **Three larger welding sparks and resizing** — integrated industrial-chain-button-v4.png with three larger spark clusters. Frame uses percentage width/height and object-fit fill, following each button rather than fixed image dimensions. Added resizable preview sections and source assertions for width/fit. Centre alpha is 1/255 (near-transparent); component/CSS/path checks pass. Final resized visual acceptance pending.

- [ ] **Corner alignment and Industrial chain revision** — Magical flourish now occupies upper-right/lower-left to match its actual geometry. Industrial replaces the machinery slabs and old frame with one newly generated transparent chain loop containing dark cogs and welding sparks. Preview regenerated; asset existence, theme/opacity checks, no-slab and corner-position assertions pass. Awaiting visual acceptance.

- [ ] **Stronger Special button artwork** — user accepted the direct-image preview as much better. Enlarged the individual frame borders, added larger real blossom/cog/rune corner details, warm Industrial metal tint, and extra toolbar spacing. Opacity-zero and ordinary-theme exclusion plus asset/component/CSS checks pass. Updated navigation-preview.html; visual acceptance of the stronger revision remains pending.

- [ ] **Reviewable game refresh candidates** — implemented a shared picker for Library/Preview Troubleshoot field actions, interactive full refresh and bulk game refresh. Initially shows up to five deduplicated candidates; Show more expands in batches of five and searches additional existing providers until exhausted. Icon/banner single-select, screenshot multi-select, description review, explicit Apply/Cancel, current-value context, failed-image handling, request timeouts, cancellation and bulk Skip/Stop. Game identity/launch/PIN fields are never included in a refresh patch. Mock-provider regression tests, JSX parsing and seven launch-safety checks pass. Live provider and packaged interaction verification remain pending; new-import background enrichment and the separate Tools workflow are unchanged.

- [ ] **Verify rebuilt per-button navigation artwork** — replaced the masked CSS-variable path with NavButtonArtwork: explicit theme/opacity props and document-relative HTML image URLs on each Home/Library/Wall/Tools button. Opaque centres protect labels; paired illustrations wrap four edges. Saved settings were Magical / decoration 100% / FX maximum, ruling out low FX for the reported screenshot. Actual-component static rendering checks pass for three themes, four buttons, default/full/zero opacity, ordinary-theme exclusion, asset URLs and production CSS compilation. Preview: desktop-app/tmp/navigation-preview.html; regenerate with node scripts/preview-navigation.cjs. Browser rendering/build remain blocked by spawn/access errors and unavailable browser connector. Exact cause of the prior invisible result is not runtime-confirmed; visual acceptance remains OPEN.

- [x] **Startup-only Fungist welcome** — reserve “Welcome to NEO-LIB” for one completed intro per desktop window. Welcome notices cannot enter the normal alert-audio queue or fall back to a generic cue, so a harmless re-render, returned notice, or renderer repaint cannot greet the player again while the launcher is already running. Source-validated; rebuilt Windows behavior verification remains required.
- [x] **Library dropdown foreground and readability repair** — force launcher, Sort, Categories, and Refresh menus onto one 10,000-level foreground plane above the scrollable game tree, overriding lower component utility layers. Their menu surface now uses a near-solid theme-aware background with strong blur, so category names and game rows cannot bleed through choices. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Special navigation button-border correction** — remove the oversized illustration spanning the whole Home/Library/Wall/Tools bank. Each nav control is now an opaque foreground button with its own small, transparent decorative corner: leaf/blossom for Anime, cog/rail for Industrial, and runic sparkle for Magical. Labels, icons, badges, and click targets deliberately sit above the artwork. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Special navigation ornament visibility repair** — decouple the small static button decorations from the global FX level. They now respond directly to the Special-decoration slider and hide only at 0% or during Rest Mode, so lowering particles for performance cannot make the intended button art vanish. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Battle.net Re-fetch apply repair** — an exact maintained Blizzard result now applies immediately when the player presses Re-fetch info on a Battle.net game. This removes the generic accept-preview detour that could make a correct Warcraft III or World of Warcraft refresh look as if nothing happened; manual re-search remains a reviewable choice. Field-specific refreshes also carry the product ID and cannot be diverted through an old Steam app ID. Source-validated; rebuilt Windows behavior verification remains required.
- [x] **Battle.net metadata recovery** — carry local `launcherProductId` into import and Re-fetch metadata lookups; make Battle.net identity outrank any inherited Steam app ID; map Warcraft III / Reforged product signals and World of Warcraft variants to a maintained Blizzard record; use the live Blizzard product page for current art/copy when reachable; and retain stable Blizzard title, publisher, description, website, genres, and tags rather than falling into a fuzzy store result when it is not. A deliberate Re-fetch bypasses the short page cache. Source-validated; rebuilt Windows verification remains required.
- [x] **All-launcher news intelligence** — remove the Steam/GOG/itch-only Home, News, favourite-alert, and Preview eligibility gate. Keep their existing direct public feeds first; then give EA App, Epic, Ubisoft, Battle.net, Riot, Xbox, Rockstar, standalone, and F95-style entries a bounded official-site-first public-news lookup with a clearly-labelled web-discovery fallback. Cache each game/window for six hours and rotate fresh checks through larger libraries so search traffic stays polite and NEO-LIB remains responsive. A search hit is news discovery only, never claimed as a verified launcher update. Source-validated; rebuilt Windows/network verification remains required.
- [x] **Library popup foreground repair** — Sort, launcher filter, Categories, Add, and Refresh now live in explicit foreground toolbar layers, while their panels use one 100-level popup plane. Category rows and Library content can no longer paint through or sit above an open control menu. Existing portal-driven context menus and modal dialogs already use higher global layers. Source-validated; rebuilt Windows interaction verification remains required.
- [x] **Home private-lock status notice** — Home now displays a compact, visible private-lock notice whenever at least one PIN category remains locked, even if it has no games. It plainly says that stats and news from those games stay hidden until the player unlocks them in Library. The notice disappears immediately after all private categories are unlocked. Source-validated; rebuilt Windows visual/interaction verification remains required.
- [x] **Special navigation illustration-frame repair** — stopped relying on a pseudo-element that could disappear behind the navigation chrome. Anime, Industrial, and Magical now mount their generated transparent frame as a dedicated, pointer-safe DOM layer around the full Home/Library/Wall/Tools button bank. Tabs stay above it and clickable; the same Special-decoration slider, FX level, and Rest Mode still govern visibility. Source/asset-validated; rebuilt Windows visual verification remains required.
- [x] **Preview action-strip FX balance** — softened the dark strip behind Launch and the game actions from a heavy 36% surface to a restrained 24% surface with lighter blur. Buttons retain their own clear contrast while more of the active theme FX and backdrop can be seen through the surrounding strip. Source-validated; rebuilt Windows visual verification remains required.
- [x] **First-PIN Lock private guidance** — the first-time PIN confirmation now introduces the readable Home top-right **Lock private** button in plain language. It explains that it immediately re-locks every PIN category, redacts protected content, and returns to a safe Library view; it also makes clear that a player re-enters their existing PIN later rather than losing or changing it. Source-validated; rebuilt Windows interaction verification remains required.
- [x] **Library toolbar readability** — Add, Wizard, Visuals, launcher filter, Sort, Categories, Auto-sort, and Refresh now use a denser themed control surface. Their opened menus use an almost-solid panel layer, keeping labels and choices readable above bright textures and animated artwork without flattening the whole Library. Source-validated; rebuilt Windows visual verification remains required.
- [ ] **Screenshot-led visual acceptance audit** — do not treat broad source/CSS work as visually complete. Rebuild the desktop app and inspect every user-called-out surface: Special button frames, navigation, Preview actions, title actions, Settings, Home, Library, Wall, category controls, mascot dock, FX at each motion/effect level, and reduced window widths. Log each result as accepted, needs correction, or blocked; only accepted items may be marked visually finished.
- [x] **Tools metadata intelligence** — Tools now use a dedicated software resolver instead of the game/Steam importer: selected-executable icon and Windows file identity first, recognised software/vendor profiles second, then one bounded public official-software lookup and Open Graph artwork fallback. Add Tool and Re-fetch info both give an approval view for name, publisher, version, category, description, source, and evidence; existing incomplete Tools receive a fill-only batch when Tools opens. No tool is launched, installed, uploaded, or overwritten by the automatic fill pass. Source-validated; rebuilt Windows visual/network verification remains required.
- [x] **Home weekly focus pass** — folded the former movable Your Week card into Top 5 Played’s fixed summary strip (hours, games touched, today, library total), then gave What Should I Play the released half-width slot with denser readable recommendations. The fixed weekly news rail is now deliberately more prominent: stronger accent border/lighting, larger icon/headline, clearer context, and larger readable story cards. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Private category lock cleanup** — reduced locked shelves from repeated lock symbols to one visible lock control and a clean Unlock {category} label. The protected game count is no longer exposed until the player unlocks that category. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Fungist speech-bubble cleanup** — routine spoken lines now use one compact, text-sized bubble with a small tail. Removed the oversized caption/glow treatment and duplicate spoken text inside an attention card; substantial cards remain only for notices that need an actual player action. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Wall private-category unlock controls** — Wall now keeps every PIN category out of the grid while adding one explicit padlock button per locked category at the top. Each button opens the normal PIN flow for only that category; its games appear in the Wall only after a successful unlock. Source-validated; rebuilt Windows interaction verification remains required.
- [x] **Unified category-header scale** — Category text size now governs the full category header as one system: category name, launcher/client badge, count, dot, arrow, drag grip, and options icon scale together from the Visuals slider. Removed independent vertical offsets so header content shares one centred baseline. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Protected category visual identity** — locked PIN categories now honour the selected Category marker mode too: Dot displays their coloured marker and Backdrop displays their coloured stripe. Counts, games, and launcher/client badges remain hidden until the correct PIN is entered. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Library text-boundary repair** — the resizable Library, its scroll tree, each game row, and the game-title column now enforce horizontal containment. Titles may use two natural lines, but cannot paint into adjacent rows or outside the Library pane; unusually long full titles remain available through their hover title. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Fungist hover-bubble boundary repair** — replaced the browser-controlled mascot tooltip with a compact in-app hover bubble that anchors above Fungist. Dock clamping now accounts for the companion and hover-bubble footprint at narrow/short window sizes, keeping both within the app viewport. Source-validated; rebuilt Windows visual verification remains required.
- [x] **Home private-game privacy layer + Lock private** — Home now converts every game belonging to a currently locked PIN category into a non-identifying protected record before any dashboard surface reads it. Rankings, sessions, best games, Chronicle, news, update cards, storage rows, cached paths, and tooltips preserve safe activity numbers but replace title/art/platform/path/details with a large lock + category tile and generic wording; protected routes cannot open a Preview, news page, downloads, or folder. Unlocking restores the source record immediately. Home also has a readable red-outlined **Lock private** button: it re-locks all PIN categories, selects a random game outside every private category, switches to safe Library Preview, and re-filters Wall/Home in the same state update. Source-validated; rebuilt Windows privacy/interaction verification remains required.
- [x] **Fungist background-app diagnosis** — PC-use alerts now include **Check background apps**, a player-requested on-demand Windows snapshot that reports the leading CPU/RAM users outside NEO-LIB and highlights only confidently recognisable game/client process names. It works for an unimported game such as Overwatch without injection, overlays, account access, game-memory inspection, process changes, or new background polling. Source-validated; rebuilt Windows interaction verification remains required.
- [x] **Home compact paired-card layout** — Recent Sessions + My Best Games, Your Week + Gaming Chronicle, and Library Health + Storage share side-by-side half-width cards where the viewport allows. Every card stays independently hideable/reorderable; drag live-reflows the grid and shows a horizontal or vertical insertion line before release. Compact screens retain a readable single-column fallback. Source-validated; rebuilt Windows interaction verification remains required.
- [x] **Special button-frame artwork** — replace faint/placeholder decoration with purpose-made transparent frames designed specifically around navigation and Preview action controls: Anime blossom vine, Industrial gear/hazard machinery, and Magical rune/crystal art. Artwork stays visibly slider-governed but sits behind every label and hit target. Source/asset-validated; rebuilt Windows visual verification remains required.
- [x] **Foreground-fog repair** — found and corrected the permanent window-edge glow at z-index 1000, above the entire launcher. It was pointer-safe but could visually veil controls and text. It now remains on the FX plane below NEO-LIB’s explicit foreground UI. Source-validated; rebuilt Windows visual verification remains required.
- [x] v1.7.5 release identity + patch notes: move the Electron package, visible app version, Settings fallback, GitHub-facing README, in-app What’s New, progress status, and release queue to **1.7.5 — Library Refinement, Safety & Fungist**. The detailed changelog keeps major features first and fixes/adjustments beneath them. Source-validated; a rebuilt Windows app is still required before release publication.
- [x] v1.7.5 Home section architecture: keep Top 5 Played fixed above Home with a smaller, fixed **This Week’s News** rail directly underneath; then group cards into **Play & history** (recommendations, week, recent sessions, ratings, Chronicle), **News & updates** (game updates and releases), and **Library & PC care** (Library Health, Storage). Add intentional section spacing, separate drag handles for section ordering, and scoped pane dragging so a card can reorder only within its own group. Split the prior Storage & Chronicle card without losing old hidden-state intent. Source-validated; rebuilt desktop visual/interaction verification remains required.
- [x] v1.7.5 Released This Week fallback repair: combine SteamSpy momentum with Steam’s official current New Releases shelf; rank Major releases first, then Noteworthy early-momentum releases, then a clearly-labelled Popular new-release fallback. Preserve bounded requests, verified full-game/release-date checks, and transparent criteria text; do not fill Home with an exhaustive store dump. Source-validated; rebuilt desktop/network verification remains required.
- [x] v1.7.5 Library regular-weight correction: make the default Library game/category weight true 400 regular rather than semi-bold 500; retain Fat as the deliberate 800 emphasis option, scoped exclusively to Library typography. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Special-art visibility repair: give Anime, Industrial, and Magical illustration assets an explicit full-screen UI-safe composition layer and stacking level rather than relying on an unpositioned ambient wrapper; enlarge/strengthen the art and compatible control ornaments at normal FX values while preserving the Special-decoration slider, FX 0, and Rest Mode removal. Source/asset-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Special navigation-frame repair: stop trying to compress full transparent illustration assets inside individual tabs. Apply Anime blossom-vine, Industrial machinery, and Magical rune art to the complete Home / Library / Wall / Tools button bank, visibly surrounding it while tabs stay layered above, clickable, and readable. Keep the shared Special-decoration slider, FX 0, and Rest Mode behavior. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Special Preview-action frame: extend the same real Special-theme art to the game Launch/action strip as a controlled, subtle outer-edge decoration. Place actual controls above it so no label, hit target, or Launch action is obstructed; retain the same slider, FX 0, and Rest Mode rules. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Preview glass rebalance: reduce the dominant Preview surface/blur treatment to roughly 80% strength across the principal reading, media, identity, detail, stats, and action surfaces, letting background animation/art remain visible without compromising readable copy or controls. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Category Dot marker repair: prevent launcher logo labels from replacing the active Dot marker. Give Dot mode an independent, visible, correctly layered slot after the collapse arrow and before the launcher label; preserve Backdrop and None behavior. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Visuals control-board layout: replace the tall masonry popover with a structured three-lane desktop grid—Object sizes & Layout, Text & category, FX—using solid themed section tabs and responsive two/one-lane fallback. Widen and clamp the draggable popover so it stays entirely inside the application. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Global UI foreground contract: place the title bar, Library/Main workspace, deals, wording, menus, and interactive controls on explicit foreground planes above all ambient/theme FX; separately raise Sidebar content above its local artwork/texture while preserving the resize handle. Keep FX pointer-safe and background-only. Source-validated; rebuilt desktop visual verification remains required.
- [x] v1.7.5 Remembered Fungist dock: add a bounded click-hold drag perch in Fungist' lower-right home zone, persist its local offset in `fungistDockPosition`, suppress chat clicks after a drag, and calculate alert/chat/launch flights as temporary transforms that always return to the saved perch. Source-validated; rebuilt desktop interaction verification remains required.
- [x] v1.7.4 Fungist voice integrity audit: verified all 27 mapped MP3 clips exist under the packaged public voice path with non-empty payloads, and verified every runtime path is relative-base safe for Electron. The companion is intentionally restrained: major PC alerts wait five minutes, normal PC checks fifteen minutes, and news/update reactions six hours; chat, tutorial, launch, action-completion, support, and Settings previews supply the remaining event-driven lines. `Take a look`, `Please don't mute me`, and `Be my guest` remain deliberate manual previews/future hand-off lines rather than background chatter. Source-validated with launch-safety verification; a rebuilt Windows audible pass is still required before claiming device-level playback verified.
- [x] v1.7.4 external-game Rest Mode handoff: when Fungist reports elevated/high PC use, he can run a player-requested path-only scan for a known NEO-LIB game started from Steam, Battle.net, Epic, or another client. A detected title gets a clear **Enable low usage until it closes** choice rather than an unexplained rest. Once enabled, the watcher checks the exact process every eight seconds while active and restores NEO-LIB automatically after it exits; idle scans stay at thirty seconds. Source-validated; rebuilt Windows behavior test remains required.
- [x] v1.7.4 Library-only typography controls: Visuals now offers five system font choices plus independent Fat and Cursive toggles for Library game/category names only. Home, Preview, Tools, and Settings remain untouched. Source-validated; rebuilt visual test remains required.
- [x] v1.7.4 generated Special-theme illustration assets: replace the earlier procedural pixel/gradient ornaments with three transparent, pointer-safe illustration assets—Anime blossom vine, Industrial machinery/hazard detail, Magical rune arc. They decorate ambient space plus compatible navigation/preview controls, follow the existing Special-decoration slider, and vanish in Rest Mode. Source/asset-validated; rebuilt visual/performance test remains required.
- [ ] Fungist optional web research: product decision queued. A safe implementation needs an explicit opt-in, a real research provider with a player-owned/configured key, visible source links, clear sent-data boundaries, a rate limit, and a no-web local-chat fallback. Do not silently give Fungist internet access.

- [x] v1.7.4 Special-theme control decoration: expand theme-owned decoration beyond distant background actors into pointer-safe navigation/game-control ornaments—Anime blossom-vine leaves/curls, Industrial hazard rails/rivets, and Magical rune arcs/star dust. Bind all to the existing Special decoration slider and per-theme FX level, with full Rest Mode removal and no label/click interference. Source-validated; rebuilt desktop visual/performance verification remains in the release gates.
- [x] v1.7.4 compact Settings container: fix the shared modal viewport-max-width precedence that overrode intended modal widths; give Settings a dedicated 43rem cap sized around four 148px theme columns plus breathing room, while preserving small-window clamping and drag boundaries. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 Library text and Preview gallery: remove the dark bordered/fill/shadow nameplate behind Library titles while retaining readable wrapping; move Preview back against the Library side; keep story/details left; and add a dedicated right-side verified-art gallery (up to eight existing header/background/hero/screenshot/cover images) without breaking prose into an image parade. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 clickable launcher update cards: replace the generic external bridge—which intentionally blocks `steam://` protocols—with a dedicated fixed Steam Downloads native action for Preview and Home pending-update cards. The action cannot launch a game; it supplies a visible failure message when a launcher has no safe downloads route. Source-validated; rebuilt Windows interaction verification remains in the release gates.
- [x] v1.7.4 Windows-aware Optimize settings: detect Windows 10/11, release, and build from Windows; use the official Game DVR/Captures URI for Background capture rather than a generic Gaming route; show OS-specific Settings paths for capture and power; and honestly mark Hardware-accelerated GPU scheduling unavailable on pre-2004 builds. Source-validated; rebuilt Windows 10/11 routing verification remains in the release gates.
- [x] v1.7.4 mascot safety and Home continuity: turning off Show NEO-LIB mascot now mutes companion voice lines as one bundle, explains exactly where Fungist can be restored, and remembers the previous voice preference; switches use one aligned control rail; the left Library gets a restrained FX-aware theme-art layer; and Home persists the last completed update result through Game Preview navigation until a newer detection replaces it. Source-validated; rebuilt desktop visual/interaction verification remains in the release gates.
- [x] v1.7.4 Anime artwork + dark-surface comfort: replace the Anime SVG/manga line-art overlay with an original finished 2D illustrated shrine-and-cherry-blossom backdrop, retaining a quiet UI-safe centre; lift only dark-theme base and box surfaces from pitch black to restrained charcoal/navy/graphite/wine gray without altering existing accent colours. Source/asset-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 Special-theme personality: move Anime into Special; rename Colorful to Magical without breaking saved `colorful` selections; add one Settings visibility slider for optional subtle Anime petals, Industrial cogs/rivets, and Magical spell arcs/sparks. Decorations remain background-only and shut down with Effects/Rest Mode. Source-validated; rebuilt desktop visual/performance verification remains in the release gates.
- [x] v1.7.4 Rest Mode clarity + tutorial/title-bar polish: explain in Optimize and Game Ready that NEO-LIB appearing in a snapshot while browsing is normal and that a tracked game pauses non-essential background work; stabilize the live tutorial navigation callback so Preview does not flicker after setting writes; unify Feedback, Discord, and Coffee into one restrained theme-aware title-bar action family. Source-validated; rebuilt desktop interaction/visual verification remains in the release gates.
- [x] v1.7.4 Library row readability + New marker: give both Library row layouts a compact accent-edged full-name plate that wraps rather than truncating a title; mark fresh imports/manual additions New and clear it only after the player deliberately opens that game from the Library into Preview; safely treat existing libraries as already seen on migration. Source-validated; rebuilt desktop interaction/visual verification remains in the release gates.
- [x] v1.7.4 Launchers hub rename: replace the active Friends surface with Launchers, providing direct normal-client shortcuts for supported locally detected launchers rather than friends/social wording; Steam now opens its normal client surface instead of its Friends page, existing local custom paths remain compatible, and new saves use the clearer launcher-path setting. Source-validated; rebuilt desktop interaction verification remains in the release gates.
- [x] v1.7.4 category-arrow scale correction: bind the category expand/collapse glyph to the exact category-text slider value and scale its click target proportionally around it. Source-validated; rebuilt desktop visual verification remains in the release gates.

- [x] v1.7.4 update-trust regression: fixed the false-update path where a Steam manifest-confirmed current game was scanned a second time as an independent public-web game. Steam’s current/pending manifest result is now authoritative; other known launcher imports are explicitly recorded as launcher-managed instead of being guessed from loose local/web evidence; and standalone/repack installs keep the independent path only when they have no launcher identity. Source-validated; a rebuilt Windows check must confirm current launcher games no longer appear in Home or Preview updates.
- [x] Fungist visual-art redesign: packaged a cohesive transparent 3D pose family for idle, blink, important-alert flight, sleep, and completion. Curiosity, stretch, smile, sparkles, breathing, and small parallax are layered transform effects over the matching core poses. No real-time 3D engine, GIF/video loop, canvas, or background polling was introduced; Rest Mode still removes the whole companion. Source/asset-validated; desktop visual verification remains in the release gates.
- [x] v1.7.4 release blocker — Launch button: root cause was a fragile Electron window-level input timestamp that could reject a genuine visible Launch click, and the renderer then returned silently. The isolated preload now grants one short-lived authorization only from a trusted press on `[data-neolib-launch]`, while the main process retains startup quarantine, one-time token, cooldown, cross-window lock, and generic-protocol block. Source-validated; a rebuilt Windows test must confirm one deliberate launch works while unattended startup launches nothing.
- [x] v1.7.4 Fungist liveliness pass: slightly increase the companion’s resting size and add more sparse, transform-only idle variation so he feels alive without introducing a heavy GIF/video loop, extra polling, or Rest Mode activity. Source-validated; desktop visual verification remains in the release gates.
- [x] v1.7.4 Fungist supplied voice-library integration: package all 18 player-provided MP3 lines under package-safe local paths; map welcome/tutorial, news, favourite game update, NEO-LIB update, PC attention, deliberate launch, guided action/explanation, chat acknowledgement, thinking, and recoverable failure interactions; add voice-only toggle, volume, and preview map; ensure one active line, shared cooldowns, UI-sound/No Sound compliance, and full Rest Mode silence. Source-validated; a rebuilt Windows playback pass remains in the release gates.
- [x] v1.7.4 motion-cadence refinement: **Balanced** is now smooth rather than hard-stepped and reduces expensive decorative particles, edge glow, blur/layer count, and ambient opacity while remaining visibly closer to Full than Calm. Normal UI feedback remains unaffected. Source-validated; desktop visual verification remains in the release gates.
- [x] Metadata-source research: IGDB is the strongest future optional canonical metadata provider (genres, companies, releases, covers, screenshots, tags; local caching permitted) but a monetized NEO-LIB requires visible attribution and a commercial partnership. RAWG is capable but its current Business plan begins at $149/month and requires backlinks; MobyGames is cost-prohibitive; TheGamesDB is a key-based secondary/retro fallback; SteamGridDB is artwork-only. No provider has been integrated without a product/credential/attribution decision.
- [x] v1.7.4 startup safety gate: the freshly installed EXE completed a 111-game background update scan in 93 seconds with no accepted or blocked launch request and no game opened. The original library-order mass-launch regression is verified fixed.
- [ ] v1.7.4 live-test follow-up: startup scanning passed across 111 games without a launch request. Fungist was hidden because two old TrackMania `ConvertAssistant.exe` helper processes matched a legacy wrongly imported launch target. Source now filters helper EXEs from imports and Rest Mode and repairs an existing helper target from launcher identity evidence. Requires one rebuilt startup check confirming Fungist remains visible and TrackMania resolves to `ManiaPlanet.exe`.
- [ ] v1.7.4 Fungist voice playback pass: rebuild locally and confirm all 18 packaged MP3 paths resolve in the Windows app; test post-intro welcome, tutorial introduction, favourite news/update, NEO-LIB update, yellow/red PC attention, a deliberate game launch, a guided action, “Why am I seeing this?”, chat acknowledgement/thinking/failure, mute, volume, No Sound, and Rest Mode silence. Do not recommend release until this audible pass is complete.
- [ ] v1.7.4 startup / viewport / FX recovery playback: source fixes now make intro completion one-shot, defer tutorial/voice until intro exit, defer Fungist greeting until tutorial close, clamp tutorial plus every current draggable app surface to the viewport, and retire legacy global FX-off switches. Rebuild locally and verify: intro exits once, step 7/8 controls remain clickable, every listed panel stays visible when opened/dragged, Low–Max FX and particles visibly return on several themes, and Fungist appears after intro/tutorial without Rest Mode.
- [x] v1.7.4 Categories control centre: replace the ambiguous quick action with a visible on/off shelf switch inside a Categories menu; add a simple category manager; guarantee category removal never removes games; move removed-category games to Uncategorized; retain Private protection in bulk removal; and migrate tag display to compact gameplay-first balanced tags. Source-validated; desktop interaction verification remains in the release gates.
- [x] Confirmation-first launcher imports: every supported launcher now requires an explicit scan confirmation. A second comparison says how many detected games were already imported and offers only truly new entries; fully imported launchers state that nothing will be added. Running-launcher detection now routes to this same Wizard instead of silently bulk importing. Source-validated; desktop interaction verification remains in the release gates.
- [x] Storage Control audit and usability pass: validate every configured launch target before measuring its parent; reject known launcher/shared-root mistakes with a visible reason; surface exact measured roots, file counts, truncation status, mod split, and a reliable Open folder action in a scrollable all-results list. Source-validated; live desktop verification remains in the release gates.
- [x] Update Intelligence reliability pass: completed a per-game evidence resolver with richer bounded local Version/JSON/config/XML/manifest reads, official-first source confidence, source/evidence ledger fields, launcher-managed diagnostics, exact-input scan caching, and targeted Home **Resolve checks** actions. A Preview can no longer receive a different game’s cached result, and a blocked/protected update page now remains visibly unresolved rather than silently clean. Source-validated; a rebuilt Windows check must confirm individual update statuses and Home resolution behavior.
- [x] Fungist post-intro greeting: reintroduced only after the prior no-click 111-game startup scan passed. Once the intro overlay has fully left, Fungist uses a renderer-only welcome bubble and friendly raised-hand 3D pose, briefly scales up, then returns to normal. It never calls launch, scan, external-link, or launcher APIs and yields to real health/news alerts. Source-validated; a rebuilt Windows startup check remains required.
- [x] Stop-ship startup root fix and launch safety: replace the unsafe PowerShell `-Command` + game-path executable-version probe with a fixed encoded reader whose target is inert process data; delay both automatic update scans until after boot; and keep the one-time Launch-button authorization, fresh native input gate, startup quarantine, cross-window rate lock, single-instance lock, protocol block, and intro input isolation. `launch-safety.log` records startup plus every allowed/blocked launch request. A mandatory prebuild verifier now guards the fixed reader and launch handshake against regression. Source validation is complete; a real Windows 45-second no-click start and one deliberate-launch check remains required.
- [x] Visual motion cadence control: add a global three-step Visuals slider—Full (default), Balanced, Calm—that retains the selected FX actors but deliberately reduces ambient animation update cadence and Framer backdrop speed. Pair it with Effects intensity in one FX performance card and explain that lowering either—or both—can improve performance. Keep UI feedback/menus unaffected and Rest Mode authoritative. Source-validated; live desktop verification remains in the release gates.
- [x] v1.7.3 Genre Intelligence refinement: treat broad-only identities as incomplete; migrate all existing source evidence to taxonomy v2; retain more direct Steam community tags; map Builder/Sandbox, Roguelike Dungeon Crawler, Survivors-like, Roguelike Shooter, construction, vehicle building, and related exact provider terminology; add deterministic tag-combination refinement; and provide Enrich source tags from both Auto-sort and Tidy Up. Source-validated; desktop interaction verification remains in the release gates.
- [x] v1.7.3 mascot sleep presence: add an ultra-light breathing loop, sleeping aura, and drifting zZz treatment so Fungist remains alive while resting without a GIF/video loop. Source-validated; desktop interaction verification remains in the release gates.
- [x] v1.7.3 finish pass: strengthen Home’s vibrant gray/blue/gold contrast and text edges; give Fungist a livelier low-cost idle presence; fix tutorial mascot overlap and Visuals close/flicker; add a protected Categories/Flat List quick switch; and surface public independent-game patches as an honest comparison-needed alert when the installed version cannot be read. Source-validated; desktop interaction verification remains in the release gates.
- [x] vNext ratings migration: reset all stored personal ratings for the new decimal rating system, then render dynamically filled fractional stars.
- [x] v1.7.3 test-fix: detect Store-installed NVIDIA Control Panel/NVIDIA App through Windows Start-app registration and replace an earlier managed Windows Graphics Settings fallback on the next launch.
- [x] v1.7.3 test-fix: darken the Mid Home theme into a more comfortable gray workspace and make Category Backdrop padding track the category text closely.
- [x] v1.7.3 test-fix: correct Update Intelligence so missing version evidence is never recorded as current; add bounded Battle.net `.build.info` detection and automatic official/web source discovery before any manual page setup is suggested.
- [x] v1.7.3 test-fix: stop Safe Junk Review from retriggering its scan after rendering results, and reduce low-value background monitoring cadence (system health 15s; external executable-path check 30s).
- [x] v1.7.3 test-fix: enlarge Home’s visual hierarchy and reserve a control gutter so Home drag/hide buttons cannot overlap pane headings.
- [x] v1.7.3 test-fix: audit and consolidate Gemini metadata fallback onto one current model and output contract; show real failure feedback and add an explicit Settings connection test.
- [x] v1.7.3 icon refresh: rebuild the supplied neon power artwork as a multi-size Windows icon for the app, EXE, taskbar, installer, desktop and Start shortcuts; assign the matching stable Windows app identity; safely notify Explorer after upgrade without clearing a cache/pins/data; and package a dedicated transparent tray mark for installed or development assets.
- [x] v1.7.3 Fungist core: add an optional, static-by-default event-driven companion with a local transparent asset; small attention bubble for favourite news, verified favourite updates, ordinary NEO-LIB updates, and yellow PC status; centre-flight only for red PC status; a tutorial introduction; Rest Mode silence; no duplicate system polling; and explicit-send Gemini chat with no automatic PC/library/account data attachment.
- [x] v1.7.3 Fungist life pass: add compact stand, blink, fly-with-neon-trails, and completion poses; low-impact docked motion and sparse true pose swaps; six controlled synthesized sound cues (Good Ding, Hey, Welcome, Warning, Attention, Completed Ding); and sound-pack/UI-sound/Rest Mode safeguards.
- [x] v1.7.3 Fungist FX + expression pass: add smile, shocked, and sleep poses; subtle medium-activity sparkles; green/yellow/red status glows; and an outward heavy-alert firework treatment around the centre-flight event.
- [x] v1.7.3 Fungist reactive behaviour + controls: make health/news/update/completion/idle reactions independently switchable, reset PC alerts after real recovery, and add a visible current-AI-model selector backed by a safe model allow-list for Fungist chat and AI metadata work.
- [x] v1.7.3 Fungist attention hygiene: add per-type cooldowns, a persistent bounded Inbox and Quick Settings on right-click, a “Why am I seeing this?” explanation for live alerts, and action-labelled completion feedback.
- [x] v1.7.3 test-fix: resolve Fungist pose artwork using package-safe Vite paths, make the companion chat visibly explain Gemini key readiness, darken Home into a readable gray workspace, and add a soft dark edge to Home’s bright/coloured copy.
- [x] v1.7.3 readability follow-up: give the Game Ready title and green/amber/red CPU/RAM status values a stronger dedicated shadow for legibility over all theme surfaces.
- [x] v1.7.3 onboarding + mascot follow-up: replace the simple tutorial with a live guided tour of Library, Preview, Tools, Visuals, Home, Game Ready, and Fungist; rename companion settings to NEO-LIB Mascot, add a visual Fungist/current + Coming soon picker, and raise the companion above Friends.
- [x] v1.7.3 mascot recovery fix: move visibility to the NEO-LIB Mascot area with a clear restore switch and ensure right-click → full mascot settings preserves Fungist visibility.
- [x] v1.7.3 Fungist Oracle chat: replace the one-answer box with a bounded persistent local scrollable conversation; clear the composer on send; preserve recent context for a coherent next reply; add Clear; and tune the AI to a concise cheerful mystical Oracle voice with longer detail only on request.
- [x] v1.7.3 wording-fit standard: replace clipped category/interface wording with natural word-boundary wrapping, preserving the full meaning and aligning nearby counts/controls to the first line.
- [x] v1.7.3 Battle.net metadata reliability: canonicalize Blizzard install identities before fetching, reject weak cross-store matches, continue automatically through public sources and optional AI, and enrich a confirmed result with official public artwork where available.

## Queued — next update

- [x] v1.7.4 release-tree cleanup: remove the obsolete root build log and unreferenced duplicate mascot sleep asset, ignore ordinary generated logs, and retain all required source, active assets, voice clips, theme art, and build-safety scripts for commit. Source-validated; no app behavior changed.
- [x] v1.7.4 feedback fallback: when a packaged build has no optional private Discord relay, replace developer-facing environment-variable instructions with a clear GitHub-report fallback that opens a prefilled issue containing only player-entered text plus version, theme, and platform. Source-validated; rebuilt desktop interaction verification remains in the release gates.
- [x] v1.7.4 Preview identity placement: move Game Identity and its genre/tag groups back inside the About this game card at the top-right of the description; enforce a strict separate prose column from compact desktop widths upward, so text ends before the panel begins and never overlaps or continues beneath it; reserve the right Preview column for game media only. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 Settings recovery + Preview gallery scale: restore the established shared modal layout after the Settings-specific sizing route rendered a blank gray shell; add a Settings-only recovery boundary so a future settings-control error cannot blank NEO-LIB and instead offers return-to-Library plus bug report; enlarge the desktop Preview media column to 460–560px (about twice the prior image area) while retaining a readable details column. Failed Fungist AI requests now offer an honest Feedback reminder and direct **Report a bug** action. Source-validated; rebuilt desktop visual/interaction verification remains in the release gates.
- [x] v1.7.4 compact theme-tile standard: set every Settings theme tile to one label-safe 148px width based on the longest current name plus breathing room; keep labels fully visible and allow safe modal-contained horizontal scroll on narrow windows. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 theme-swatch motion calm-down: slow Settings theme-bar colour motion to one restrained 48-second drift so the picker remains readable and calm. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 Wall route repair: remove the redundant normal-Library route after Wall selection, which reset `libraryViewMode` from Wall straight back to Preview. Source-validated; rebuilt desktop interaction verification remains in the release gates.
- [x] v1.7.4 Fungist ground-FX alignment: separate the pulse animation from fixed centring so the neon pad and centre streak remain directly under the companion. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 mascot opacity + Preview media calm-down: remove the half-transparent pose-entry animation that made Fungist flash on blink/speech/idle swaps; retain his glow and movement. Restore a compact source-owned Preview carousel capped at two images and remove the repeated screenshot breaks inserted throughout prose. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 Fungist overlay + library-aware Oracle: replace the cheap circular mascot pod with a transparent living-character overlay, reactive neon ground pad, aura, hover sparkles, and mood/action FX. On manual AI questions, provide only a bounded visible-library summary of names/tags/ratings/playtime (no paths, saves, accounts, processes, or locked Private entries). Add locally resolved `Launch {game}` and `Launch a random {genre} game` commands with a named guarded confirmation button—never automatic launch. Source-validated; rebuilt Windows visual, command, consent, and launch-safety verification remains in the release gates.
- [x] v1.7.4 Fungist visible speech delivery: give every real voice clip one shared, package-safe speaking event with the exact transcript and mood; show it in a readable speech bubble beside Fungist with matching happy/thoughtful/concerned/urgent/celebratory motion; mirror the same delivery in the Tutorial overlay. Cover automatic alerts, chat, completion, launch, support, and Settings previews so no companion voice is unexplained background audio. Source-validated; rebuilt Windows voice/visual playback remains in the release gates.
- [x] v1.7.4 Home theme composition repair: remove all-text outline/shadow treatment from normal neutral copy; retain a soft dark edge only for coloured status/accent text; rebuild Home around a calm medium-gray room, darker slate panels, clean light-gray copy, restrained light-gray structure, and blue/gold signal/FX accents. Source-validated; rebuilt desktop visual review remains in release gates.
- [x] v1.7.4 Fungist fluidity pass: enlarge the docked mascot from 98px to 118px, raise it slightly above the permanent rail, make transform-only resting motion smoother, shorten and randomize blink cadence, and bring smile/curiosity/stretch/sparkle/greeting gestures closer together. Preserve the existing no-video/no-canvas/no-extra-poll design and full Rest Mode shutdown. Source-validated; rebuilt desktop visual/performance verification remains in release gates.
- [x] v1.7.4 Mascot sound-settings cleanup: remove the redundant synthesized Fungist soundscape preview from Settings while retaining global UI Sounds/Sound Pack and the companion’s focused voice toggle, volume, and local line-preview map. Source-validated; rebuilt desktop Settings visual verification remains in release gates.
- [x] v1.7.4 Preview composition pass: retire the redundant standalone image carousel and center the game information inside a less-transparent, bounded reading panel. Preserve the game’s own verified screenshots as editorial breaks within the description and leave animated theme art visible around the panel edges. Source-validated; rebuilt desktop visual verification remains in the release gates.
- [x] v1.7.4 workspace and mascot finish: make Home the only dashboard route; restore last-played/first game for Library and last-used/first tool for Tools; provide an honest empty Tools state; keep Wall as Wall until a tile deliberately opens Preview; extend Wall through 9×9 and 10×10 with centered landscape/source-wide art; add a PIN-safe `Unlock {category} ({count})` control; move Fungist above chat in a ready pose; make him cheer toward the actual Launch button after a confirmed manual launch; package/map the nine additional supplied voice clips; and make theme artwork visibly present at normal FX levels. Source-validated with the launch-safety gate; rebuilt Windows visual, audio, and interaction verification remains in release gates.

- [ ] Optional metadata-provider integration decision: if approved, add a provider card in Settings that lets the player connect their own IGDB application credentials, makes the required attribution visible, keeps credentials local, queues/caches requests politely, and uses IGDB only as a confidence-ranked source behind exact launcher identity. Keep TheGamesDB/SteamGridDB as separately opt-in secondary fallbacks; do not add RAWG/MobyGames by default.

- [x] Future mascot FiFi asset foundation: created a separate nine-pose, real-alpha supporting kit (idle, greeting, thinking, alert, sleep, flight, celebration, listening, and concern) plus an event/motion map. The live articulated rig is now selectable without replacing Fungist.
- [x] FiFi live animation rig — source complete: separate illustrated body/fin/tendril assets, digital blinking/gaze, breathing, fin/tendril movement, twelve reactions, motion/FX/rest controls, a reusable React adapter, local studio and shared host integration are implemented. Installed visual/interaction acceptance is tracked near the top of this queue.

- [x] Library category-collapse clarity: replaced the tiny chevron with a solid, high-contrast 28px expand/collapse control, with its own direct click target and an unlock action for Private shelves. Source-validated; desktop visual verification remains in the release gates.
- [x] Smart Library sort: added a quiet theme-aware Sort control with Custom order, Alphabetical, Date added, My rating, Most played, and Last played. It sorts each category while shelves are on or all visible games in Flat List, keeps stable ties, and preserves locked Private exclusion. Source-validated; desktop interaction verification remains in the release gates.
- [x] Library Cover Wall: added a top-pane **Wall** view switch that clears the persistent Preview into a cover-and-name game grid. Clicking a tile opens the normal Preview; locked Private tiles remain excluded; and a responsive 3×3–8×8 density slider keeps titles and covers readable. Source-validated; desktop visual verification remains in the release gates.
- [x] Fungist supplied English voice: the supplied local 18-line library is now packaged and event-mapped with opt-in mute/volume/preview controls. A future generated-speech provider remains separate and must be opt-in, privacy-clear, credential-backed, and never automatically send chat/library content.
- [ ] Theme readability pass: source contrast audit completed for all current palette tokens. Vaporwave Day, Daybreak, Mint, Modern, and Home now have dedicated readable accent-text companions; coloured status words have a stronger edge; and Home has a darker gray card/base treatment. Still requires a rebuilt Windows visual check against Home cards, Library rows, Preview, menus, status colours, and reduced window sizes before this item can be closed.
- [ ] Theme atmosphere pass: **source work complete for every current theme.** Synthwave, Vaporwave Day, Daybreak, Home, Mint, Midnight, Ocean, Crimson, Anime, Gaming, Modern, Industrial, Colorful, Generic Gray, and Generic Blue now have original low-contrast, full-window artwork beneath all UI. Art fades with FX level, only uses a slow transform drift outside Calm, and is absent in Rest Mode; existing theme-specific particles remain in place. A rebuilt Windows app must still verify readability, all window sizes, FX levels, motion modes, and actual resource cost before this item can close.
- [x] Preview editorial-layout pass: description copy now uses a labeled reading card with steadier paragraph rhythm and release context. Longer entries can insert narrow visual breaks from that game’s own already-fetched screenshots only—never random Google or generated imagery. Empty data retains an honest Re-fetch route. Source-validated; a rebuilt desktop visual check remains in the release gates.
- [x] Refresh / Library Maintenance redesign: Refresh now offers targeted missing/older metadata maintenance or a separately confirmed full refresh. The target count is calculated before either begins, manual overrides stay excluded, the queue carries on after an individual lookup error, and the Library shows compact live progress. Tidy Up remains a separate diagnosis/repair route. Source-validated; a rebuilt desktop interaction check remains in the release gates.
- [ ] Auto-sort precision overhaul: **source flow complete.** Auto-sort proposes at most four shelves only after three direct genre-profile matches, avoids broad Adventure/Simulation fall-through, lets the player opt each shelf in/out, shows every planned game assignment with per-assignment exclusion, caps a game at two new shelves, lets the player select exactly which incomplete games receive genre enrichment, deliberately reuse a matching category or create a separate `Auto · …` shelf, and has a one-transaction Undo that restores its prior category assignments and removes newly created empty shelves. A rebuilt desktop interaction check must still validate representative racing, RPG, builder, roguelike, and visual-novel examples. Never remove a game/category or silently create a shelf.
- [ ] Fungist post-intro reliability pass: after the theme work, verify in the rebuilt Windows app that Fungist visibly spawns shortly after the intro completes, plays the larger welcome motion, shows the welcome notice without overlapping controls, then returns to the normal resting position. Repair any timing, visibility, or Rest Mode edge case found during that check.

- [x] Onboarding refresh: replace the first-run tutorial with a short, current walkthrough of importing games, privacy/Hidden, Home, game Preview/Customize, and Rest Mode; keep it skippable and practical.
- [x] Unified hover tips: add a shared delayed (one-second) tooltip treatment and apply clear descriptions across the primary launcher controls, actions, Home panes, and important status indicators.
- [x] Preview news imagery: enrich the in-library game news bar with each article’s own image when safely available; otherwise use the selected game’s existing hero, cover, or screenshot as a local visual fallback. Preserve a clean text-only layout only when no suitable artwork exists.
- [x] New Mid theme — Home: a calm light-gray workspace with crisp black/white structure, light-blue edges and FX, and a warm-yellow/gray comfort blend pattern.
- [x] Storage Control bugfix: ignore non-file launcher targets, deduplicate shared install folders so identical disk sizes are not repeated, keep completed results when returning to Home, and speed up folder-only scans with a small bounded parallel pass.
- [x] External-launch Rest Mode detection: safely monitor ordinary Windows process paths for a NEO-LIB library game started from its original launcher; never treat an idle launcher as a game. The current v1.7.4 handoff presents a clear player-controlled low-usage choice before resting.
- [x] Library compact spacing: make the gap between the Library header and first game reduce cleanly at the minimum spacing slider value, while preserving collision-safe spacing with all text/row-size slider combinations.
- [x] Category backdrop scaling: make the category Backdrop option scale proportionally with category text size, including its padding/radius/glow relationship.
- [x] First-launch window geometry: default the width to 75% of the usable display and height to 90%, while retaining a user-resized/restored window on later launches.
- [x] Update status ledger: persist a read-only per-game latest-version check result so known-current games are actively filtered out of Preview and Home update surfaces; retain only verified available/pending updates and explicit insufficient-evidence guidance.
- [x] Background update intelligence: on library startup and after a game launch, inspect every eligible game through layered, bounded local version evidence (launcher manifest, saved version, executable/folder naming, version/changelog/readme/config files), then compare only against its official/public update source. Show a clear blinking update bar above Preview news when a verified newer version exists.
- [x] Post-play rating prompt: after a meaningful completed play session, gently invite the player to give the game a decimal personal rating; respect dismissal/snooze choices and never repeatedly nag for the same game.
- [x] Optimize Center: add an **Optimize** entry beside the CPU/RAM footer that opens a theme-aware power-up dashboard with two action rows and persistent last-run summaries.
- [x] Speed Up Gaming: provide an on-demand view of the top five CPU and memory consumers, safe confirmed closing of non-protected programs, GPU activity/process attribution when Windows exposes it, and clearly explained Windows gaming settings (Game Mode, GPU scheduling, power plan, capture/background recording, and other worthwhile checks) with honest pros/cons and safe actions.
- [x] Safe Junk Review: scan bounded cache/log/crash-report locations plus likely leftover installers and archives near configured game folders; show every result with an open-folder action, require physical inspection guidance and two confirmations before deletion, protect live game/save/application folders, and summarize reclaimed space without claiming unverified savings.
- [x] GPU setup assistant: on first desktop launch, identify the installed GPU(s), add a native vendor control-center shortcut when available (with Windows Graphics Settings as an honest fallback), and add managed GPU-Z / CPU-Z utilities to Tools.
- [x] Managed GPU-Z / CPU-Z tools: render missing utilities as clearly unavailable, offer a compact Locate-or-Install menu, validate a manually located executable, and download only from the official TechPowerUp/CPUID sources after explicit user action. GPU-Z will be installed as its official portable executable; CPU-Z will open its official interactive installer rather than use a hidden or silent install.
- [x] Battle.net metadata enrichment: use reliable local Battle.net identity clues before the normal public metadata sources so installed Blizzard games receive correct artwork and descriptions.
- [x] Friends Hub polish: make the panel slightly larger, increase text size slightly, and reduce transparency slightly.
- [x] Theme taxonomy and balance: move Generic Gray and Generic Blue from Special to Mid; make Generic Blue visibly lighter.
- [x] Genre intelligence: retain specific provider tags such as Third-Person Shooter, Action Roguelike, Co-op, and Multiplayer alongside the core genre instead of reducing games such as Risk of Rain 2 to only Action.
- [x] What should I play?: increase the pane/card scale and add clearer, richer personal reasons to play each recommendation.
- [x] Independent/repack update intelligence: investigate why older non-launcher games do not surface after Refresh; fix the scan timing/evidence path so explicit installed-vs-latest information is checked safely without claiming unsupported launcher data.
- [x] Home Top 5 Played redesign: promote it into its own centered top pane; keep hide/restore, but lock it against Home reordering.
- [x] Home reordering redesign: replace the current drag interaction with a modern live-reorder experience—active glow, dimmed peers, animated movement before drop, and a visible insertion indicator.
- [x] Library Health redesign: remove it from This Week, restore it as its own Home pane, and add useful health detail beyond the headline score.
- [x] After source work: run full source audit, update GitHub/in-app patch notes and progress, then perform desktop and package validation gates.

## Deferred verification for v1.7.3

- [ ] Run desktop interaction checks for the completed v1.7.3 source work in an unrestricted Windows session, including a real itch desktop install fixture.

## Verification and release gates

- [ ] **Installer upgrade-safety check for v1.7.7** — validate an ordinary in-place NSIS upgrade from the public version: same `com.neolib.app` identity, retained library/settings/private categories, no app-data deletion, and no duplicate install. This is intentionally an upgrade test, not a forced-uninstall design; clean uninstall remains only a recovery path for manually copied, mismatched, or broken local packages.
- [ ] Run desktop interaction tests for the metadata repair queue, genre approval, Home update pane, game-preview update alert, and launcher imports. Blocked here because Chromium cannot create its Windows platform channel under the managed process sandbox (`Access is denied`).
- [ ] Run the normal Vite/Windows package build in an environment that permits child processes. Direct renderer/Tailwind compilation passes; Vite and electron-builder are blocked here at `esbuild` / `app-builder.exe` with `spawn EPERM`.
- [x] Perform a requirement-by-requirement source completion audit against this queue and the carried-forward v1.7.2 scope.
- [x] Confirm GitHub-facing notes and the in-app changelog match the v1.7.3 source-validated build. Recheck after the external desktop/package gates before recommending push/release.

## Blocked — required evidence/dependency

- [ ] No code blocker. A real local itch desktop install is still needed to validate the new receipt-marker importer end-to-end. Catalog-level itch account/library access remains intentionally out of scope rather than opening the client’s live database.

## Completed and source-validated in the current batch

- [x] Approval-first metadata/genre repair queue with repaired/skipped progress and Stop Review.
- [x] Local title clues from executable, nearby folders, and bounded README/title fields.
- [x] Steam pending-download detection from local launcher manifests, shown in Home and Game Preview.
- [x] Independent-game installed-version/update-page watching with explicit version-label checks.
- [x] Multi-entry independent-game patch-history extraction and an in-app read-only history view.
- [x] Native GOG installed-game import and launcher category.
- [x] Native EA App/legacy Origin installed-game import and launcher category.
- [x] Native Ubisoft Connect installed-game import, launcher identity, native launch URI, and category.
- [x] Native Battle.net installed-game import from verified Blizzard Windows installation records.
- [x] Native Riot installed-game import from bounded local product metadata.
- [x] Native Xbox/Game Pass import from bounded XboxGames roots and per-game MicrosoftGame.config files.
- [x] Native Rockstar installed-game import from verified Windows installation records, excluding launcher/support components.
- [x] Safe native itch.io installed-game import from the desktop app's explicitly configured install locations and completed-install receipt markers, without database/account access or broad folder scanning.
- [x] First-time launcher detection and Wizard routes audited so all supported adapters preserve their executable and launcher-specific identity.
- [x] Launcher filters, Home platform labels, and Settings version display audited for complete v1.7.3 identity/version coverage.
- [x] Single-click-outside dismissal audited across standard and custom modal surfaces.
- [x] GitHub documentation, carried-forward v1.7.2 scope, `PROGRESS.md`, and in-app changelog updated for the v1.7.3 source work.

## Explicitly not queued

- Discord friend mirroring and unsafe launcher friend-list scraping were dropped by user decision. Safe native social handoff remains the supported design.
- [x] v1.7.7 Control Center and Steam update follow-up: restore pointer access to the left-side NEO-LIB menu above layered Library artwork; clamp delayed hover explanations inside the viewport; stop completed Steam manifests with stale byte counters from showing false pending updates; and route the update action through Steam's documented Downloads handoff. Source-validated; rebuilt Windows interaction verification remains in the release gates.
